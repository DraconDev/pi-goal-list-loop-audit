# Last-day pauses + ai-auto-writer (2026-09-22)

Ledger forensics, Sep 21–22, 7 active projects. Question: are we still
getting pauses? **Yes.**

## Counts (Sep 21–22)

| project | pause-ish signals |
|---|---|
| ai-auto-writer | 12 main_model_recovery_wait, 16 fallback exhausted, 5 pause_goal_aborted_turn, 719 payload_guard_eviction |
| ai-auto-music | 27 pause_goal_aborted_turn, 20 pause_wait_clamped, 219 zombie_run_stood_down_user_input, 9 zombie suspected |
| ai-auto-video | 9 pause_goal_aborted_turn, 9 pause_wait_clamped, 5 overdue-resume |
| dracon-utilities | 4 aborted turns, 3 rearm storms |
| dracon-platform | quiet (evictions + samples only) |

## ai-auto-writer goal `20260922081842-yudpof` — currently PAUSED (blocked)

Timeline of its last day (all provider-side, none auditor-side):

1. Sep 21 20:25→23:00: opencode-go 400
   `reasoning encrypted_content was not issued to this caller`, retried
   hourly, attempts 1→9. Never recoverable by retry.
2. Sep 21 23:11: 400 `Image count 12 exceeds limit 4 per request`
   (goal attaches screenshots; provider caps at 4).
3. Sep 22 09:27: ChatGPT usage limit (plus plan), ~3h waits.
4. Sep 22 19:14: 503 overloaded on meta/muse-spark; fallback chain
   holds ONE model (`attempted=[itself]`) so cycle-reset probes the
   same dead model hourly.
5. Sep 22 20:23: `pause_goal_aborted_turn kind=blocked`. Ledger ends.

Plus: 719 identical `payload_guard_eviction` (~every 12s, same
generation/bytes, 15MB images remain) — eviction doesn't stick and the
event has no dedupe.

## Ranked improvements (new, follow-ups to RESULTS-AUDIT)

1. **Classify non-retryable 400s as fatal**: `encrypted_content` and
   image-count errors must trigger fallback/abort, not hourly retry.
2. **Clamp images per request** (count, not just bytes) to the
   provider limit before send.
3. **Single-model chain**: when the fallback chain has length 1,
   skip the cycle-reset probe theatre; wait quietly and say so.
4. **Dedupe payload_guard_eviction** per generation while values are
   identical.
