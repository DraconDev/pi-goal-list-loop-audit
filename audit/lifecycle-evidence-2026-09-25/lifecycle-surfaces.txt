detached audit lifecycle — user-facing surfaces per required state
rendered 2026-09-25T18:11:10.247Z by tests/evidence/render-lifecycle-surfaces.ts
every line below is produced by the real extension code; nothing is hand-written copy.
==============================================================================

── starting ────────────────────────────────────────────────────────────────
STARTING — the claim is durable, the worker has not reported yet
durable projection: state=starting phase=starting stale=false terminal=false lastEvent=claim-stored
next action:       detached worker starting — /goal cancel discards the claim
The launch window is a real state, and a claim with no worker event never has its age back-filled into activity.

widget card:
  ⟡ detached audit lifecycle… · auditing · total 0s
  ├─ auditor: starting · detached worker · last progress none yet
  │ next: completion review applying
  └─ waiting for completion review

status line:
  glla: MAIN HOST · SUPERVISING · auditor ✓ starting ▱▱▱▱▱

/goal status:
  (a detached worker owns this claim, so it only exists inside a live session; the same claim with no live worker is the recovery-needed panel below)

── running ─────────────────────────────────────────────────────────────────
RUNNING — durable last activity, refined by the live worker
durable projection: state=running phase=running stale=false terminal=false lastEvent=worker-activity
next action:       completion review applies automatically
A restarted session reads `lastActivityAt` from the ledger, not from a HUD that no longer exists.

widget card:
  ⟡ detached audit lifecycle… · auditing · total 0s
  ├─ auditor: last observed tool · detached worker · last progress 22s ago
  │ last tool: bash
  │ next: completion review applies automatically
  ├─ report stream muted — final text at completion review
  │  evidence: report stream observed
  └─ 22s in detached worker · worker activity 22s ago

status line:
  glla: MAIN HOST · SUPERVISING · auditor ▶ last observed tool ▰▰▰▰▱

/goal status:
  (a detached worker owns this claim, so it only exists inside a live session; the same claim with no live worker is the recovery-needed panel below)

── settling ────────────────────────────────────────────────────────────────
SETTLING — the approval is durable, the terminal archive is owed
durable projection: state=settling phase=settling stale=false terminal=false lastEvent=verdict-applied
next action:       approved; the terminal archive is owed — /goal resume finishes it
The worker-complete snapshot that used to rename this 'awaiting completion review' can no longer mask the durable phase.

widget card:
  ⟡ detached audit lifecycle… · auditing · total 0s
  ├─ auditor: settling · detached worker · last progress 4s ago
  │ next: completion review applying
  ├─ audits: 1 review · last approved 30s ago
  ├─ latest: verdict applied
  │  evidence: final report
  └─ waiting for completion review · worker activity 4s ago

status line:
  glla: MAIN HOST · SUPERVISING · auditor ✓ settling ▰▰▰▰▰

settlement (fresh session):
  archive:    test-1790359870252-y3z4cx.md (landed)
  claim:      released
  ledger:     audit_settlement_resumed, audit_settlement_completed, audit_settlement_restart_check
  queued terminal render (user-facing):
    ## Done — shipped.
    
    — 0s elapsed · 1 audit
    
    ### What Changed
    1. **lifecycle projection + settlement driver.**
    2. **audit/DETACHED-AUDIT-LIFECYCLE-2026-09-25.md.**
    
    • completion audit approved after session recovery (1 review).
    • record: .pi-glla/archive/test-1790359870252-y3z4cx.md

── approved ────────────────────────────────────────────────────────────────
APPROVED — settlement durable; the summary is in .pi-glla/archive/
durable projection: state=approved phase=settling stale=false terminal=true lastEvent=verdict-applied
next action:       settlement durable — the summary is in .pi-glla/archive/
Only the settlement driver may claim this state, and only after the archive landed.

widget card:
  (none — the goal is archived; its user-facing surface is the terminal render below)

status line:
  (none — the goal is archived)

settlement (fresh session):
  archive:    test-1790359871197-nir8bc.md (landed)
  claim:      released
  ledger:     audit_settlement_resumed, audit_settlement_completed, audit_settlement_restart_check
  queued terminal render (user-facing):
    ## Done — shipped.
    
    — 0s elapsed · 1 audit
    
    ### What Changed
    1. **lifecycle projection + settlement driver.**
    2. **audit/DETACHED-AUDIT-LIFECYCLE-2026-09-25.md.**
    
    • completion audit approved after session recovery (1 review).
    • record: .pi-glla/archive/test-1790359871197-nir8bc.md

── recovery-needed ─────────────────────────────────────────────────────────
RECOVERY NEEDED — an interrupted or unknown claim, with a real next action
durable projection: state=recovery-needed phase=recovery-pending stale=false terminal=false lastEvent=worker-activity
next action:       /goal resume retries the stored claim
A stale attempt is never rendered as finished, and the surface names the command that unblocks it.

widget card:
  ⏸ detached audit lifecycle… · paused · total 0s
  ├─ auditor: recovery needed
  └─ parked 9m 01s ago · /goal resume retries the stored claim

status line:
  glla: ⏸ recovery needed · /goal resume retries the stored claim · queue empty · safely parked · owner: manual resume · last host activity not observed · next: /goal resume

/goal status:
  /goal status → Completion audit: recovery needed — /goal resume retries the stored claim
  /glla status  → goal [goal] paused (audit recovery needed): detached audit lifecycle · five required states · one settlement

── blocked-settlement ──────────────────────────────────────────────────────
RECOVERY NEEDED — approved, but the terminal archive was refused
durable projection: state=settling phase=settling stale=false terminal=false lastEvent=parked
next action:       approved; the terminal archive is owed — /goal resume finishes it
The promise in the pause text is real: /goal resume finishes the approved settlement, with no new audit.

widget card:
  ⏸ detached audit lifecycle… · paused · total 0s
  ├─ auditor: settling
  │  verdict applied 46s ago · approved; the terminal archive is owed — /goal resume finishes it
  ├─ lifecycle: safely parked · owner: manual action · queue empty
  │  last host activity not available · next: /goal resume
  ├─ blocked — waiting for manual action
  ├─ completion approved (openai/gpt-test), but the terminal
  │  archive failed — an existing archive refused the write
  ├─ saved — 1 audit · resumes exactly here
  │  Fix .pi-glla disk access or resolve the archive fence, then
  │  /goal resume finishes the approved settlement. No new audit
  ├─ is needed.
  └─ audits: 1 review · last approved 30s ago

status line:
  glla: ⏸ settling · approved; the terminal archive is owed — /goal resume finishes it · queue empty · safely parked · owner: manual action · last host activity not available · next: /goal resume · 1 review · last approved 30s ago

/goal status:
  /goal status → Completion audit: settling — approved; the terminal archive is owed — /goal resume finishes it
  /glla status  → goal [goal] paused (audit settling): detached audit lifecycle · five required states · one settlement — completion approved (openai/gpt-test), but the terminal archive failed — park-archive

==============================================================================
no state above renders an unresolved audit as terminal completion;
the recovery action named by every parked/owed state is a real command.
