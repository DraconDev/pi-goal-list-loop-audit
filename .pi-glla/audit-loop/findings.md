# Audit findings — pi-goal-list-loop-audit (2026-08-11, v0.34.127)

Collected by the [LIST-AUDIT-COLLECT] pass — four parallel Explore surveys
(lifecycle, orchestrator/auditor, UI/menu, tests/tooling/docs), ~35 tool uses
each. Append-only. No code was changed by this pass.

## FIX findings

- [x] FIX: MEDIUM: runLoopTick captures `const loop = state.loop!` (extensions/goal-loop.ts:425) and mutates it across `await runMeasure` (up to 10 min) / `await runGit` — a concurrent `/loop stop` or main-model park replaces state.loop, so the tick's final-iteration mutations land on the discarded object, the durable record loses the last measurement, and finishLoopGit can git reset/commit against a stopped loop's branch — fixed in d467b908 (tick rebinds the active loop after every await via rebindLoop(); missing/inactive loop abandons the tick with a `loop_tick_abandoned` ledger entry)
- [x] FIX: MEDIUM: already_shipped guard's version-less path is pure phrase-trigger with zero corroboration (extensions/loops/goal-tools.ts:507-519) — fixed in d3808e6f (v0.34.128: version-less claims route to the NORMAL audit with routedToAudit:true + verify-in-tree label in the audited recap; version-bearing claims keep the abort; tests updated; full suite 1241 pass/1 skip/0 fail) — any unanchored "already shipped" / "no new work shipped" in completionSummary aborts the goal and skips the auditor and reviewer entirely; a legit summary using shipped-as-synonym phrasing becomes terminal status=aborted and the completion audit never runs
- [x] FIX: MEDIUM: fan-out dedupe false-drop (extensions/loops/goal-orchestrator.ts:793-796) — dedupes first-60-chars of each finding against the JOINED queue, so a distinct finding whose prefix appears inside a longer queued item is silently dropped (no ledger of the suppression); `alreadyQueued = open.length - fresh.length` (line 797) miscounts cap-cut items as already queued — fixed in 60e9f124 (v0.34.129: per-item canonical finding-prefix matching; true queue matches separated from cap-deferred items via `deferredByCap`; regression tests cover substring collision + 51-item cap; full suite 1243 pass/1 skip/0 fail)
- [x] FIX: MEDIUM: "read-only" auditor is not read-only — bash is in READ_ONLY_TOOLS and the worker launches pi with --approve (extensions/goal-loop-auditor-process.ts:96, scripts/goal-auditor-worker.mjs:9); the prompt only asks the model not to mutate, so repo content under audit can inject instructions into a privileged write-capable agent; a hung bash emits no RPC events so neither the inactivity brake nor the parent's 60s fresh-heartbeat fires — the audit is pinned to the full 30m wall — fixed in 5f1e9c8f (v0.34.130: detached worker allowlist is read/grep/find/ls only; `--no-approve` replaces `--approve`; unexpected tool events fail closed; parent and worker enforce an independent five-minute per-tool timeout; prompt treats repository content as untrusted data; regression tests cover CLI argv, bash rejection, and both timeout paths; focused auditor suite 24 pass/0 fail; full suite 1247 pass/1 skip/0 fail)
- [x] FIX: MEDIUM: VISION_ASSIST_GUIDANCE claims "gpt-5.5, sonnet, opus are forbidden by default" (extensions/vision-assist.ts:46) — the default is [] since v0.34.115 (extensions/goal-loop-core.ts:1029); agent-facing policy text misstates the gate, so an agent will refuse/sanction model switches on a policy that does not exist — fixed in f40d9dc (vision guidance and settings now state that the default forbidden-model list is empty; explicit lists remain gated)
- [x] FIX: MEDIUM: blocked-pause banner mislabels the main-model-recovery manual hold as "blocked — waiting on a non-quota condition" (extensions/goal-loop-display.ts:1216) — holdMainModelRecovery (extensions/goal-recovery.ts:305-315) sets pauseKind "blocked" with a quota reason, so the same card shows "owner: main-model recovery" above a banner denying quota (v0.34.64 design comment violated) — fixed in caf3e3eb (main-model recovery manual holds now render "manual recovery hold — automatic probes stopped" in widget/status; ordinary blocked pauses retain the non-quota banner; regression test added; pause/display tests 108 pass/0 fail)
- [x] FIX: MEDIUM: forbidden-models picker exclusion only exact-matches `provider/id` (extensions/model-picker.ts:62-64) while callers pass unexpanded normalizeModelRefs output (extensions/main-model-recovery.ts:43-58) — documented substring-style entries ("sonnet" covering "anthropic/claude-sonnet-4-5", goal-loop-core.ts:1043-1047) are NOT hidden in the backups/subagent-chain pickers; user can pick a forbidden model into a chain (runtime gate still skips it — UI promise breaks, policy holds) — fixed in b44e1f0 (picker and headless list filtering now use case-insensitive substring matching; regression coverage for raw `sonnet` policy entries and typed backup input; focused model/settings suite 54 pass/0 fail)
- [x] FIX: MEDIUM: docs/VISION-ASSIST.md:35,57 claim `/glla forbiddenModels=...` and `/glla visionAssist=off` syntax; the dispatcher rejects any non-action arg as "Unknown /glla action" (extensions/goal-commands.ts:1924-1947) — same false-claim class as the fixed "/glla thinking=" (v0.34.127) — fixed in b7c4a58 (docs now use `/glla` → Keep-going settings navigation; regression test rejects invalid argument-style syntax; focused vision-assist suite 27 pass/0 fail)
- [x] FIX: MEDIUM: AGENTS.md:59 references `scripts/release.sh` which does not exist (scripts/ contains only goal-auditor-worker.mjs + smoke.sh) — dead path in the durable operating rules — fixed in b265310 (AGENTS.md now points to `npm run release:check` and `.github/workflows/publish.yml`; scripts/release.sh no longer referenced)
- [x] FIX: MEDIUM: tests/README.md documents the runner as `node --experimental-strip-types --test tests/*.test.ts` with a node-only coverage model, but package.json "test" is `bun test` and behavioral-orchestrator/loops tests DO drive the extension handlers via MockPi; also stale "168 unit tests" count (v0.21.1) — fixed in 0e7725d (docs now identify Bun as the supported runner, describe MockPi behavioral coverage and the live smoke boundary, and avoid a fixed test count)
- [x] FIX: MEDIUM: package.json "test:node" script is dead AND broken — `tests/*.test.ts` glob misses tests/loops/goal.test.ts and ~71 test files import `.js`-extension paths that only exist as `.ts`; keeping a broken runner next to the real one invites future misuse — fixed in ccb01a5 (removed the broken `test:node` script; `test`/`test:all` remain the supported commands)
- [x] FIX: MEDIUM: v0.34.127's headless `/glla` list sync (11 keys: decisionPopup, carryover, auditorModelFallback, auditorSameSessionSwap, auditorSilent, auditorProgressSignals, hourlyQuotaProbe, subagentModelStrategy, subagentModelOverrides, subagentFallbacks, toolOverrides) has NO regression test — tests/settings-menu-complete.test.ts:222 pins only stallEscalationRefires + wedgeAlertMinutes; the thinking-row half is tested (model-picker.test.ts:191-205), the sync half is not — fixed in f2a7cf7 (headless fallback test now isolates cmdSettings's list and asserts all eleven keys plus existing stall-brake rows; focused settings/model suite 42 pass/0 fail)
- [x] FIX: MEDIUM: tests/commit-survival-e2e.test.ts:25-47 is vacuous — even with GLLA_E2E_DAEMON=1 it runs in a fresh mkdtemp repo that no daemon watches; the reflog/HEAD asserts are trivially true and can never detect daemon rewrites — fixed in d6292d9 (requires an explicit GLLA_E2E_DAEMON_REPO listed by dracon-sync, creates a configured-identity empty probe commit, waits 12s, and checks reachability/reflog rewrite evidence; default run skips safely)
- [x] FIX: MEDIUM: t.skip() vs assert.ok(true) contradiction (tests/command-registration-collisions.test.ts:218 vs tests/subagent-model-override.test.ts:167) — if bun's node:test lacks t.skip(), the live-rig test throws whenever ~/.pi/agent is absent; if not, the drift guard's assert.ok(true) workaround is a silent no-op — fixed in 81f7d33 (both optional tests now use Bun-compatible test-level skip options; missing drift source is an explicit skip, upstream no-pins branch retains a real assertion and returns)
- [x] FIX: LOW: cmdTweak reinstalls a pre-dialog goal snapshot (extensions/goal-commands.ts:670) — state.goal captured before `await ctx.ui.confirm`; any state.goal change made while the dialog was open (user pause, heartbeat park, audit-recovery mark) is silently reverted by the stale spread — fixed in 21c17c9 (cmdTweak rebases onto the latest same-goal state and cancels safely if the goal was replaced or finished; concurrent-pause regression test added in 1c6a46a)
- [x] FIX: LOW: parseQuotaError numeric Retry-After regex unanchored and runs before the HTTP-date branch (extensions/quota-retry.ts:97) — a digit-leading date value (`retry-after: 2026-08-10T12:00:00Z`) parses as ~34 min instead of the absolute reset; the date branch is unreachable for that shape — fixed in 22d5131 (whole-value Retry-After parsing distinguishes delta-seconds from digit-leading dates; regression coverage added)
- [x] FIX: LOW: continuationStartRecoveryRearms map (extensions/goal-continuation.ts:262) has no clear site (unlike continuationStartCompactionRearms) — entries accumulate per dispatch inside the 5-min post-recovery grace → unbounded growth over a long session — fixed in ba8f5b5 (dispatch watchdog cleanup deletes the settled record's recovery counter; destructive reset clears any orphaned entries; regression guard added)
- [x] FIX: LOW: compaction-cap fall-through drift (extensions/goal-continuation.ts:612) — comment promises "fall through to the unacknowledged warning" after the 3-rearm cap, but code reaches dispatchTimedOut → retryContinuationDispatch, re-sending and adding 60 s before the user-facing warning fires — fixed in a3651b09 (compaction and recovery caps now dispatch the unacknowledged warning directly; bounded regression coverage verifies no retry is sent)
- [x] FIX: LOW: TEMPORARY_QUOTA regex bare `over\s+(?:the\s+)?(?:quota|rate\s*limit)` alternative (extensions/quota-retry.ts:43) matches non-temporary plan-wall prose ("You are over your monthly quota — upgrade") and classifies it rate-limit instead of plan-quota, contradicting the comment's explicitly-temporary scope (label/display-only; retry envelope is uniform) — fixed in 363aae6a (removed the bare over-quota alternative; regression coverage preserves the plan-quota classification while temporary wording remains rate-limit)
- [x] FIX: LOW: syncSubagentModelOverrides repair state (extensions/goal-loop-subagents.ts:221-271) records only files written on the LAST sync — after an idempotent no-op sync, prevWritten is empty and the "repaired" notification can never fire again for a later external modification — fixed in 81b47b0 (persisted state now tracks all currently managed files, including idempotent no-op files; regression coverage verifies later deletion still reports repaired)
- [x] FIX: LOW: matchedVersion misattribution (extensions/loops/goal-tools.ts:514) — grabs the FIRST vX.Y.Z anywhere in the summary, not the version the "already shipped" claim refers to; a dependency bump ("v2.1.0") or changelog quote produces stopReason `already_shipped:v2.1.0`, mislabeling the abort — fixed in f9397f3 (only explicit claim-adjacent syntax binds a version; unrelated versions before or after the claim fall through to versionless audit; regressions cover both directions)
- [x] FIX: LOW: already_shipped branch (extensions/loops/goal-tools.ts:522-537) mutates + archives WITHOUT the guardGoalBeforeContinuation/archive-fence the normal path gets at line 563 — a crash between updateGoal (terminal stopReason on a still-active goal) and archiveCurrentGoal leaves a live "active" goal; the archive write also unconditionally overwrites an existing archive of the same id — fixed in 7735fbb1 (branch now fences before archiving and archives the terminal snapshot without the active-stopReason gap; archiveCurrentGoal uses an exclusive temp+link destination and preserves existing same-id archives; guard rejects interrupted active terminal markers; regressions in a6637d70/3c6ec687)
- [x] FIX: LOW: orphaned pi RPC child after cancel/wall-timeout — worker finish() SIGTERMs pi without waiting (scripts/goal-auditor-worker.mjs); if pi is wedged on a hung provider stream the worker stays alive (stdout 'data' listener refs the pipe) and both linger indefinitely — fixed in b300a761 (finish now waits for bounded SIGTERM shutdown, escalates to SIGKILL, destroys child streams, and regression coverage proves a SIGTERM-ignoring RPC child cannot outlive the worker)
- [x] FIX: LOW: .pi-glla/audit-jobs/<attemptId>/ job dirs are never removed by parent or worker (extensions/goal-loop-auditor-process.ts:206-217, 468) — every infra retry/fallback attempt accumulates a durable dir + request.json/progress.json forever — fixed in 2307f58 (the parent removes only directories it successfully created after every settled/abandoned attempt; regressions cover successful, cancelled, stalled, retry, and collision-preservation paths)
- [x] FIX: LOW: parseAuditFindingsForFanout only matches exactly-one-space boxes `\[ \]` (extensions/goal-loop-forever.ts:575) — a findings.md edited with two-space/aligned checkboxes is silently invisible to the fan-out (not queued, not counted in the "N open finding(s)" notify) — fixed in 719b25c5 (open boxes now accept one or more spaces/tabs inside the brackets; regression covers aligned spacing)
- [x] FIX: LOW: formatClockTime is production-dead (extensions/goal-loop-display.ts:58) — v0.34.125 removed its last call site; only a test references it; stale v0.34.95 comment claims the quota-recovery status line uses it — fixed in c0265fd (removed the unused formatter and stale source/test references)
- [x] FIX: LOW: stale default-policy comment: "the policy gate defaults ON — gpt-5.5/sonnet/opus must never be selected unattended" (extensions/goal-settings.ts:178) — actual default is [] since v0.34.115 — fixed in f40d9dc (settings, vision guidance, and docs now state that the default forbidden-model list is empty; explicit lists remain gated)
- [x] FIX: LOW: interactive menu's "Effective resolution" row renders without the session model (extensions/loops/goal-settings-ui.ts:513) — inherit-parent shows generic "session model" while the headless /glla list passes the real ref and shows "provider/model (inherits session)" (extensions/goal-commands.ts:1994-1997); two surfaces disagree on the same setting — fixed in 6f027316 (interactive settings now passes the active provider/model into the effective-resolution row; regression covers the real session ref)
- [x] FIX: LOW: "next: resuming now" can contradict the parked head on the same status line — pausedNextTransition returns "resuming now" once retryAt passes (extensions/goal-loop-display.ts:634) while the head renders "parked on provider wall — retrying automatically" (line 781) — fixed in f8d1c2f (provider-recovery status now stays "next: retrying automatically" until recovery state clears; regression covers a passed retryAt)
- [x] FIX: LOW: dispatcher cases `subagentFallbacks:Plan` / `subagentFallbacks:general-purpose` unreachable — the menu only emits rows for KNOWN_PINNED_DEFAULT_AGENTS = ["Explore"] (extensions/settings-menu.ts:198-206, extensions/goal-loop-subagents.ts:39); those chains are settable via settings file/headless but not editable in the menu — fixed in f6fc91c (menu now emits fallback-chain rows for every OVERRIDABLE_AGENT_TYPES entry, with Plan/general-purpose value coverage)
- [x] FIX: LOW: an exception in ANY settings editor silently exits the whole menu — `catch { return; }` (extensions/loops/goal-settings-ui.ts:521-523) swallows the error with no notify, contradicting the loud-save-failures precedent (extensions/goal-commands.ts:1445-1448) — fixed in 46fdf8b (editor errors now warn with the setting id/path and return to the menu for retry; regression pins loud notification and no silent exit)
- [x] FIX: LOW: drift guard silently passes via assert.ok(true, "drift check skipped") when pi-subagents isn't installed (tests/subagent-model-override.test.ts:169,186) — the embedded-copy drift pin never runs in CI, so EXPLORE_DEFAULT_DESCRIPTION can silently drift — fixed in 46e8fc0 (drift source is now required and missing provider data fails loudly; publish CI installs the pinned pi-subagents source before the release suite)
- [x] FIX: LOW: tests/auditor-process.test.ts:54-61 writes tests/fixtures/auditor-fake-worker.mjs into the source tree at runtime — cleaned up only on successful teardown; interrupted runs leave a stray tracked-able file — fixed in 4b545cf (fake worker is generated inside each per-test temporary directory; regression coverage in 9dabba70 confirms temp-only creation and cleanup)
- [x] FIX: LOW: jiti 2.7.0 is imported by tests/repro-jiti-state-split.test.mjs and required by "test:jiti" but not declared in package.json devDependencies — works only via hoisting from @earendil-works/pi-agent-core's dependency tree — fixed in cad6934f (jiti 2.7.0 is now an exact direct devDependency with the matching package-lock root entry)
- [x] FIX: LOW: CHANGELOG.md:3-4 "## Unreleased" contains numbered version headers (v0.34.127…v0.34.118) while package.json already reads 0.34.127 — contradicts docs/RELEASING.md's own rule ("do not invent version headers for work that was never tagged"); version/date tracking is ambiguous until the release commit — fixed in 712b8865 (untagged work now uses descriptive headings under Unreleased; tagged 0.34.116–0.34.121 milestones are level-two released sections)

## DECIDE findings (NOT queued as fixes — direction calls for the user)

- [x] DECIDED: corroborate — version-less already-shipped claims route to the normal audit so the auditor verifies the work exists in the tree (true claim completes truthfully; false claim is disapproved and the finding stays queued). Implemented as v0.34.128 in d3808e6f (2026-08-11)

## Severity breakdown

- **MEDIUM (14 FIX)**: lifecycle 1 (runLoopTick loop capture race) + orchestrator/auditor 3 (already_shipped phrase-trigger; fan-out dedupe false-drop; read-only auditor not read-only) + UI/menu 3 (VISION_ASSIST_GUIDANCE misstates policy; blocked-pause banner mislabel; forbidden-models picker exact-match) + tests/tooling/docs 7 (VISION-ASSIST doc syntax claims; AGENTS.md release.sh dead path; tests/README stale runner model; test:node dead+broken; headless /glla sync untested; commit-survival-e2e vacuous; t.skip contradiction)
- **LOW (21 FIX)**: lifecycle 6 (cmdTweak snapshot revert; retry-after regex; rearms map growth; compaction-cap drift; TEMPORARY_QUOTA overmatch; subagent sync repair state) + orchestrator/auditor 5 (matchedVersion misattribution; already_shipped no fence; orphaned RPC child; audit-jobs dirs; fan-out box regex) + UI/menu 6 (formatClockTime dead; stale default-policy comment; effective-resolution row; resuming-now contradiction; unreachable subagentFallbacks cases; silent menu catch) + tests/tooling/docs 4 (drift guard silent pass; fixture written to source tree; jiti undeclared; CHANGELOG unreleased headers)
- **DECIDE (1)**: already_shipped version-less corroboration
- [x] DECIDED: permit bash in the detached completion auditor for broader audit power and customization, accepting the prompt-injection/write-capability risk in preference to strict read-only safety (2026-08-11); retain per-tool timeouts and `--no-approve` as independent bounds

---

# Full-project audit pass — 2026-08-16 (v0.35.3, branch main)

Second full-project pass: five parallel Explore surveys (lifecycle,
commands/recovery, auditor/reviewer, UI/menu, tests/docs/CI), all collected.
Append-only; every FIX below has a real fix commit on `main` before its box
was checked. 20 FIX + 1 DECIDE (C-F1).

## FIX findings (2026-08-16 pass)

- [x] FIX: MEDIUM: scratch-branch leak on refusal paths (extensions/goal-loop.ts:725-750) — branch=1 `loop stop`/`loop start` refusals (dirty tree, already on a scratch branch, active loop) return WITHOUT restoring the original branch, stranding the user on `pi-glla-loop/...` — fixed in 0dcd7cbf (both refusal paths now call restoreOriginalBranch() before returning; stranding regression covered)
- [x] FIX: MEDIUM: branch=1 terminal-iteration destroy (extensions/goal-loop.ts:587-601,662) — the final iteration's finishLoopGit ran after the stop/park handler had already restored the original branch, so the flat/null hard-reset path (and its sibling) ran `git reset`/`checkout` against the ORIGINAL branch — fixed in 7d52dab3 (commitPendingTerminalWork() commits the terminal iteration on the scratch branch before any branch switch; original-branch work is never touched)
- [x] FIX: MEDIUM: auditMeasureCmd counts DECIDED lines as open findings (extensions/goal-loop-forever.ts) — `grep -cE '^- \['`-style counting included `- [x] DECIDED:`/`- [x] DEFERRED:` lines, so resolved direction calls kept the /loop audit well from going dry — fixed in da1273d9 (auditMeasureCmd now counts only `- [x] FIX` lines: `grep -cE '^- \[[xX]\] FIX'`)
- [x] FIX: MEDIUM: strict-space checkbox regex misses aligned/tabbed boxes (extensions/goal-loop-forever.ts) — countOpenAuditFindings/topOpenAuditFinding matched only exactly `[ ]`, so findings.md edited with aligned columns silently vanished from the fan-out and the "N open finding(s)" notify — fixed in 77a684ce (open-box patterns now accept `[ \t]+`)
- [x] FIX: MEDIUM: parseLoopStartArgs splits key=value pairs inside quoted targets (extensions/goal-loop-forever.ts) — `/loop start "reduce TODO=chores" ...` split on the `=` inside the quoted objective; unmapped `key` args were also silently dropped — fixed in da1273d9 (masked-quote spans preserved during kv splitting; unknown keys restored)
- [x] FIX: LOW: backoff dead code (extensions/goal-loop-backoff.ts) — backoffMs/shouldPauseAfterBackoff/humanMs/accountTurnForNudges + BACKOFF_HARD_CAP_MS/BACKOFF_ERROR_* had zero production call sites; the header's "hard 5-minute ceiling — beyond it the orchestrator pauses" promise was never enforced (nudge/pause decisions live in goal-loop.ts/heartbeat/core) — fixed in 58a1680b (module + 52e2cb03 tests: removed dead exports/constants and their tests; live scheduling constants kept)
- [x] FIX: MEDIUM: zombie auto-abort fires while a subagent wait is in flight (extensions/loops/goal-activation.ts:410 via HeartbeatDeps) — a MAIN session busy-silent on a long Agent tool run could be auto-paused as "zero stream activity" even though the child was alive and working; hung-children territory belongs to the subagent-hang watchdog (alerts, never auto-kills) — fixed in 44cda922 (zombie predicate stands down while hasLiveSubagentHangProbes() or an Agent/steer_subagent/get_subagent_result call is in flight; ledger breadcrumb `zombie_run_stood_down_subagent_wait` only when the abort would have fired; behavioral regression tests added)
- [x] FIX: LOW: starved-notify refires every fresh second while the streak holds (extensions/goal-heartbeat.ts) — the second-precision `lastStarvedRefusedAt !== Date.now().toString().slice(0,-3)` gate made the "/compact" warning re-fire up to once per second across a 90s starved episode; re-arm also depended on a fresh-second race — fixed in 44cda922 (per-episode one-shot latch keyed on the streak: fires once, re-arms only after isContextStarvedRefused() clears; behavioral regression test added)
- [x] FIX: LOW: quota-retry dead ternary — both branches identical (extensions/quota-retry.ts:143) — the display projection collapsed to the same string, so the sensitive/non-sensitive distinction was a no-op and the caller's redaction marker interpolated a display that could carry raw account/org text — fixed in c1f4bc20 (non-sensitive provider text now projects a bounded ≤160-char single-line display via providerDisplayText(); sensitive stays "provider error"; the audit-report redaction marker is a fixed `[provider diagnostic redacted — provider error]` tag; regression coverage added)
- [x] FIX: MEDIUM: auditor prompt claims an auto-continue the detached worker lacks (extensions/goal-loop-auditor.ts) — buildGoalAuditorPrompt told the auditor "the orchestrator's auto-continue fires on stop_reason=length", but the detached worker has no auto-continue: a length stop just ended the attempt — fixed in 33b67ab6 (prompt now states one bounded session, NO auto-continue, chunking is the only recovery; anchors still resolve)
- [x] FIX: LOW: goal.schema.json missing fields (schemas/goal.schema.json) — goal-level revision (contract-change counter consumed by the revision-bound verdict gate), completionSummary, pendingTasks (maxItems 5, aggressiveMode audit-cap TODOs) and auditVerdict-level revision/durationMs/regressionShieldMissing were written by the runtime but not documented in the schema (tokensUsed/tokensLimit were false positives — nested under usage) — fixed in 87c870f0 (all documented; JSON valid; schema suites pass)
- [x] FIX: MEDIUM: smoke.sh waits on "approved by auditor" prose that precedes the verdict (scripts/smoke.sh) — the phrase appears in tool returns/agent narration before the audit finishes, so the happy path could pass mid-audit; the disapprove branch also matched a cryptic `"isapproved"` string — fixed in f826712 (wait_for now matches `"✓ done:"` — the completion notify card, emitted only at goal-tools.ts:831 / goal-auditor-hooks.ts:1059; disapprove branch reads "Auditor disapproved")
- [x] FIX: MEDIUM: smoke.sh loop scenario uses a rejected `/glla project notify='...'` assignment (scripts/smoke.sh) — /glla is an action namespace (status/log/audits/wipe…) and rejects key=value args as "Unknown /glla action"; notifyCmd is now written directly to `$WORK/.pi-glla/settings.json` (the project settings file loadSettings reads), still project-scoped
- [x] FIX: MEDIUM: settings menu shows base defaults while aggressiveMode is ON by default (extensions/settings-menu.ts) — unset auditCap/stuckMaxInterventions rendered "5 [default]" and wedge alerts "30" although the runtime (aggressiveMode !== false) uses 10/10/0 — the table lied about what an unset key does — fixed in 6dab35b9 (fallbacks derive from resolveEffectiveAggressiveSettings(settings); the MenuDefaults injection param is gone; tests pin 10/10/0 default, 5/5/30 under explicit aggressiveMode:false, and explicit-settings-wins)
- [x] FIX: LOW: boolean VALUE column vocabulary mismatch (extensions/settings-menu.ts) — set booleans rendered as raw `true`/`false` while every fallback label and select editor says on/off — fixed in 6dab35b9 (booleans render on/off uniformly; pin moved from "true" to "on")
- [x] FIX: MEDIUM: examples/example-objective.md documents rejected `/glla key=value` syntax (examples/example-objective.md) — `/glla notify='...'`, `/glla tokenlimit=...`, `/glla model=...`, `/glla project autoresume=on` all error as "Unknown /glla action" — fixed in ce55af49+a26a1b49 (examples now point at the /glla settings table rows and the .pi-glla/settings.json project file for scripted setups)
- [x] FIX: LOW: INSTALL.md stale runner/test-count claims (INSTALL.md) — "tests use --experimental-strip-types" (they run under bun) and the v0.34.22 "728 tests" pin — fixed in a26a1b49 (bun named as the runner; count line updated and marked non-authoritative)
- [x] FIX: LOW: docs/RELEASING.md milestone example wrong format (docs/RELEASING.md) — cited `### 0.34.51`; the changelog convention is `## <version> — <title> (<date>)` with `###` topic subsections — fixed in a26a1b49
- [x] FIX: LOW: README keeps the false "stuck backoff caps at 5 minutes then pauses" claim (README.md:534-546) — the exact promise removed from goal-loop-backoff.ts as never-enforced; the real v0.27.3+ mechanism is nudge accounting (3 consecutive unproductive turns pause/stop; error/abort turns exempt) — fixed in a26a1b49
- [x] FIX: LOW: pi-subagents is an undeclared test dependency (package.json / .github/workflows/publish.yml) — the release drift guard (tests/subagent-model-override.test.ts:167) FAILS LOUDLY without @tintinweb/pi-subagents' default-agents.ts source, but local `npm test` only passed on machines with it under ~/.pi/agent/npm and CI installed it via an ad-hoc `npm install --no-save --legacy-peer-deps` step — fixed in 4e436ca (pinned devDependency @tintinweb/pi-subagents@0.15.0 — peers >=0.80.0 satisfied by ^0.84.2 devDeps, no legacy-peer-deps needed; npm ci verified clean; ad-hoc CI step removed; drift guard 12 pass against the local source)

## DECIDE findings (2026-08-16 pass)

- [x] DECIDED: remove the dead knobs (2026-08-16) — user chose REMOVE over implement: leverageMode/confirmOn fields, defaults, the "Leverage mode" menu row, and the /glla toggle branch are gone (fixed in 52d807b2); the reviewer's real leverage behavior (bug/refactor findings enqueue to /list without a Confirm) is hardcoded in runReviewer and unchanged

## Severity breakdown (2026-08-16 pass)

- MEDIUM (11 FIX): scratch-branch leak; branch=1 terminal destroy; auditMeasureCmd DECIDED counting; strict-space box regex; parseLoopStartArgs kv-in-quoted-target; zombie abort vs subagent wait; auditor prompt auto-continue claim; smoke.sh verdict-wait; smoke.sh /glla notify rejection; settings menu effective defaults; example-objective.md /glla syntax
- LOW (9 FIX): backoff dead code; starved-notify refire; quota-retry dead ternary; schema missing fields; bool value vocabulary; INSTALL.md claims; RELEASING.md example; README false 5-min claim; pi-subagents devDep
- DECIDE (1): reviewer leverageMode/confirmOn implement-vs-remove (C-F1)

---
# Full-project audit pass — 2026-08-18 (v0.35.4, branch main)

Four parallel Explore surveys were launched across lifecycle, auditor/display,
queue/drafting, and packaging/schema/docs. The lifecycle and auditor workers
hit the provider usage ceiling before reporting; the queue and packaging
reports were rechecked against the current tree. The following findings are
new relative to the append-only history above. No DECIDE findings were found.

## FIX findings (2026-08-18 pass)

- [x] FIX: HIGH: queue sidecar writes bypass the persistence-degradation boundary and can leave partial temporary files or abort a batch after earlier sidecars landed (extensions/goal-loop-core.ts:967-978; extensions/goal-commands.ts:908) — fixed in 708a4ec3, 4410bc72, f7d5f81b (atomic writes now use runPersistStep with temp cleanup, failed batches roll back their own sidecars, and callers refuse RAM mutation on failure; focused queue regressions pass)
- [x] FIX: HIGH: carryover=clear drops state.list without deleting recoverable queue sidecars, so a fresh session can resurrect items the user cleared (extensions/loops/goal-session.ts:1239-1249) — fixed in 4410bc72 (clear removes durable queue sidecars and reports failed deletions before claiming completion)
- [x] FIX: HIGH: disk-recovered queue items are used only by /list show and are never hydrated into state.list, so /list next and other mutations cannot act on the displayed recovered work (extensions/goal-commands.ts:1084-1105; extensions/loops/goal-list-queue.ts:380,646) — fixed in 4410bc72, 0b7220aa (one id-deduplicating hydration path runs before list commands and activation, persists recovered state, and preserves active exclusion)
- [x] FIX: MEDIUM: readQueueFromDisk omits repairTarget, so a recovered repair/replan item loses the safety metadata needed by guarded activation (extensions/goal-loop-core.ts:1021-1048) — fixed in 708a4ec3, 4410bc72 (validated repairTarget metadata round-trips and repair updates atomically replace the sidecar; regression covers recovery)
- [x] FIX: MEDIUM: disk queue recovery sorts by sidecar filename/random id suffix rather than durable enqueue order, so same-batch items can reorder after reload (extensions/goal-loop-core.ts:1025-1027; extensions/goal-commands.ts:849-866) — fixed in 708a4ec3, 4410bc72 (new queue items persist monotonic queueOrder and recovery uses it with a legacy addedAt/id fallback)
- [x] FIX: MEDIUM: packaged docs/INDEX.md links to tracked root docs, examples, changelog, and audit files excluded by package.json's files allowlist, producing broken links in the npm artifact (package.json:33-42; docs/INDEX.md:6-24) — fixed in e926be80 (public install/changelog/examples are shipped and repository-only audit/plan links are no longer presented as package links; npm dry-run regression passes)
- [x] FIX: LOW: README's hardcoded current package version remains v0.35.3 while package and lock metadata are v0.35.4, with no release-test assertion for the README marker (README.md:13; tests/glla-version.test.ts:1-31) — fixed in e926be80 (README now reports v0.35.4 and release-contract.test.ts checks it against package.json)
- [x] FIX: MEDIUM: the persistence schema omits runtime Goal parentId/telemetry and AuditVerdict impossible/impossibleReason/durationMs fields, while the drift test checks only schema-to-interface membership (schemas/goal.schema.json; tests/persistence-hardening.test.ts:112-119) — fixed in dda4c237 (schema documents the runtime fields and persistence-hardening now checks both interface directions plus nested verdict parity)

## Survey limitations

The lifecycle and detached-auditor scopes require a fresh provider-backed
survey to rule out additional findings; their workers were not resumable after
the provider limit. The eight findings above were confirmed directly before
being recorded.

## Follow-up finding — 2026-08-18

- [x] FIX: MEDIUM: heartbeatTick stops probing as soon as a stale completion claim is parked as `status=paused`/`phase=recovery-pending`, so the healthy same-session self-heal path never runs without a user command and the objective can remain idle indefinitely (extensions/goal-heartbeat.ts:375-390; extensions/loops/goal-session.ts:1433-1498) — fixed in d1aa6791, 243f9593, eccf0874 (parked completion claims keep the heartbeat supervision gate armed; a healthy same-session heartbeat now reaches the existing one-shot recovery path; behavioral and host-loss regressions pass)
- [x] FIX: MEDIUM: the inline `extractVerificationContract` fallback treats the ordinary imperative `verify ...` as a contract marker, truncating a list objective such as `... and verify the stale recovery fix. Done when: ...` at the word `and` and triggering faulty-objective repair (extensions/goal-loop-core.ts:1790-1805) — fixed in e29d566f (the inline parser now reserves bare `verify` for the explicit `Verify:` marker form; regression coverage preserves ordinary verify imperatives before `Done when:`)

---
# Replacement fresh-project audit pass — 2026-08-18 (v0.35.4, branch main)

Three read-only Explore surveys were launched in parallel in one message for
lifecycle/recovery, detached auditor/display, and queue/drafting. They ran on
the free KiloCode model after the initial provider-backed attempts failed
honestly with insufficient-balance/usage-limit errors. Their reports were
cross-checked against the current tree: the queue report repeated findings
already checked above and the lifecycle/auditor reports included speculative
race/leak claims that do not match the actual guards, event pairing rules, or
focused tests. The three scopes were manually rechecked; packaging/schema was
also manually rechecked against the release allowlist and parity tests.

The only genuinely new finding from this pass was the ordinary-`verify`
contract-parser truncation already recorded immediately above and fixed in
e29d566f. No additional FIX or DECIDE finding is supported by the evidence.
The provider limitations and rejected candidates are recorded here rather than
turning unverified hypotheses into findings.

---
# Full-project audit pass — 2026-08-23 (v0.35.33, branch main)

Four parallel Explore surveys (lifecycle/recovery, loop/respec,
commands/drafting/UI, tests/docs/packaging), all collected; every finding
below was independently re-verified against the tree by the orchestrator
before recording. Focus: everything shipped since the 2026-08-18 pass
(v0.35.14–v0.35.33). The user's verb-semantics question (audit vs start vs
plan) was answered inline in-session and is recorded as finding F-D1.
No code was changed by this pass.

## FIX findings (2026-08-23 pass)

- [x] FIX: HIGH: the v0.35.31 "metric never moved" stop reason promises "/loop resume retries" but that prefix is not in RESUMABLE_STOP (extensions/goal-loop.ts:929-939 matches only HELD_ON_RESTORE/provider errors/stopped by user/plateau/stalled/stuck/zero-stream), so /loop resume says "No held loop to resume"; worse, propose_loop_refine refuses when the loop is inactive, so the only recovery is /loop stop + fresh start discarding history — same class as the v0.35.25 issue-#14 zombie-prefix bug (fixed there for zero-stream, missed for the brand-new prefix) — fixed in v0.35.54: RESUMABLE_STOP now matches the "metric never moved —" prefix (resume re-arms error/stuck/stall counters, preserves iteration/best/history; refine becomes reachable again after resume, and a measure-changing refine re-scopes the era so the grace re-arms); behavioral test tests/metric-never-moved-resumable.test.ts drives the real /loop resume command against the exact production reason string (red-proven with the clause neutered) plus a negative pin that bounded stops stay non-resumable; full gate rc54 exit=0, 1557 pass / 139 files
- [x] FIX: MEDIUM: State.lastOutcome is not durable — persistStateLine (extensions/goal-state.ts:72-89) serializes a fixed field list WITHOUT lastOutcome and readState (extensions/goal-loop-core.ts:1276+) never restores it, so closeArchivedSlot's "durable … hours later" record (extensions/loops/goal-orchestrator.ts:1002) dies with the process; any restart/reload within the 24h window blanks the widget again — exactly the failure v0.35.30 was written to fix; same latent class as the documented lastCompactionAt trap (goal-state.ts:63-68); also absent from schemas/goal.schema.json — fixed in 845fcd88+78de5a8f (v0.35.34: writer always serializes the field — null when absent, since readState spreads successive events and an omitted key would resurrect stale values / block /glla wipe from clearing; reader restores through a strict shape sanitizer where corrupt lines degrade to absent; schema addition N/A — goal.schema.json documents Goal objects only, State-envelope fields like supervisorPausedAt/loadHoldAt are absent there by design; round-trip + corruption regression tests in tests/last-outcome-retention.test.ts; full gate rc34 exit=0, 1511 pass / 132 files)
- [x] FIX: MEDIUM: v0.35.31 user-seed trust silently no-ops whenever the seed carries a "Done when:" clause or @agent role — createGoal (extensions/goal-orchestrator.ts:600-615) stores the RAW arg in userSeeds but cleans role/contract out of goal.objective, while the guard requires exact equality userSeeds[i].trim() === goal.objective.trim() (extensions/goal-continuation.ts:862) — so "/goal start \"Fix X\nDone when: tests pass\"" still parks behind the suspicious-objective heuristic, the exact false-positive class the fix targeted — fixed in 33e96ee0+d871f696 (v0.35.35: new pure helper objectiveIsUserSeeded() in goal-loop-core.ts normalizes BOTH sides through the same extractAgentRole→extractVerificationContract pipeline createGoal applies; createdVia still gates WHO is trusted; guard calls it before deriveObjectiveRepair so trust short-circuits repair; tests: clause+role-bearing user seed on a heuristic-tripping prose dispatches verbatim with the ledger entry, reviewer-created goals with matching-cleaned seeds never get faulty_objective_user_seed_trusted; full gate rc35b exit=0, 1513 pass / 132 files)
- [x] FIX: MEDIUM: complete_goal newObjective appends the AGENT-authored objective to userSeeds (extensions/loops/goal-tools.ts:494-498), and since setGoal defaults createdVia="user", the v0.35.31 guard then trusts that agent-written text as explicit user prose and skips the suspicious-objective pause — an agent can launder report-garbage objectives past the fence; contrast the repair-redraft append which is Confirm-dialog gated (legitimate) — fixed in df6ecd71 (v0.35.36: userSeeds stays strictly human-confirmed text — creation arg, /goal tweak Confirm dialog, repair-redraft confirm; the agent-authored pivot is ledgered via goal_tweaked and audited against the NEW contract in the same complete_goal call; regression test in tests/revision-bound-audit.test.ts proven red on the laundering line and green on the fix; full gate rc36b exit=0, 1514 pass / 132 files)
- [x] FIX: MEDIUM: autoResumedAt/autoResumedEvent recovery notice is never marked delivered — the only clearing site is MANUAL resume (extensions/goal-commands.ts:475); heartbeat backstop (extensions/goal-heartbeat.ts:252-256) and provider recovery (extensions/goal-recovery.ts:1505) set it, continuation injects the WELCOME-BACK notice on every dispatch while status=active && autoResumedAt (extensions/goal-continuation.ts:1243-1249), so a goal running days after one recovery gets the stale directive injected forever — fixed in the sendContinuation accepted-dispatch site (v0.35.37): after dispatchAccepted, the stamp clears (autoResumedAt/autoResumedEvent → undefined) and a recovery_notice_delivered ledger entry records delivery, so the WELCOME BACK directive is injected exactly once per auto-resume; manual /goal resume keeps its clearing. Test updated in tests/wait-overdue-backstop.test.ts to the once-only contract (notice in sent payload + delivery ledgered + continuationPrompt no longer injects it) and proven red with the clearing neutered; full gate rc37 exit=0, 1514 pass / 132 files)
- [x] FIX: LOW: two loop-stop routes skip the queued-list announcement contradicting v0.35.22's "ends by ANY route … ANNOUNCE loudly": stuck-ladder stop (extensions/goal-loop.ts:657-666) and provider-error/abort cap stop (extensions/loops/goal-activation.ts:1852-1859) notify the loop line but never announce queue resumption — fixed in v0.35.41: both routes now call announceQueuedListAfterLoopEnd (stuck-ladder stop in goal-loop.ts; provider-error/abort-cap stop in loops/goal-activation.ts, which imports the now-exported function); two behavioral tests in tests/repair-card-loop-block.test.ts drive each production route against a seeded waiting queue (the stuck-ladder test fires a degenerate word-run turn — the only deterministic way to classify stuck on demand, since clean turns RESET consecutiveStuck) and both were proven red with their call neutered, green restored; full gate rc41 exit=0, 1518 pass / 132 files
- [x] FIX: LOW: applyRefinement resets bestValue/lastValue/stallCount on a measure change but keeps history (extensions/goal-loop-forever.ts:187-203), so metricHasMoved reads OLD-era improvements (goal-loop-forever.ts:263) and is permanently true for the NEW metric era — the v0.35.31 flat-reading grace can never apply after a measure-changing refine — fixed in v0.35.42: applyMeasurement now scopes BOTH movement checks (metricHasMoved and the never-moved bound — the same era-bug also shielded a dead NEW metric forever) to the current measure era via history.iteration > lastMeasureChangingRefinement.iteration (the boundary was already recorded on every LoopRefinement); two twin tests in tests/loop-forever.test.ts proven red without the scoping (false plateau from old-era improvements; never-moved unreachable after a refine) and green with it; full gate rc42 exit=0, 1520 pass / 132 files
- [x] FIX: LOW: refine's orchestrator-side spec write updates specText/specHash but not loop.specChecked (extensions/loops/goal-tools.ts:1998-2002), so the next tick ledgers spec_item_progress attributed to the agent's iteration (extensions/goal-loop.ts:563-567) — unearned progress feeding the multi-signal stuck gate — fixed in v0.35.43: the refine handler now re-baselines loop.specChecked = countCheckedSpecItems(loop.specFile) right beside the existing specHash re-baseline after the orchestrator-owned spec write, so the next tick cannot credit the agent with checkboxes that arrived via the user-confirmed respec; behavioral test tests/refine-spec-rebaseline.test.ts drives the REAL propose_loop_refine tool handler end-to-end (owner-ctx binding for the foreign-tool guard, autoAcceptDrafts project setting, restore-gate wake) asserting specFile written + specHash moved + specChecked === 4; proven red with the line neutered, green restored; full gate rc43 exit=0, 1521 pass / 133 files
- [x] FIX: LOW: draftingDepth runtime global is write-only dead state — set in startDrafting (extensions/loops/goal-list-queue.ts:770), reset in clearDraftingState, zero readers (template selection uses the local parameter) — AND five proposal-completion paths null draftingTarget directly without resetting depth (extensions/loops/goal-tools.ts:1637,1644,1702,1711,1890), breaking the "no target ⇒ normal depth" invariant the moment a consumer appears; additionally startDrafting sets both globals BEFORE beginDrafterModel which sits outside the try (extensions/loops/goal-list-queue.ts:776-826), so a beginDrafterModel throw leaves the orphaned drafting gate the function's own header warns about — fixed in v0.35.44: (1) draftingDepth removed outright — ambient var, session-local + registration, startDrafting writer, clearDraftingState reset all deleted; part 2 of the finding (five completion paths breaking the no-target⇒normal-depth invariant) is resolved BY CONSTRUCTION since the depth state no longer exists, and additionally the two bare draftingTarget=null paths that skipped the drafter-model restore (batch-activation conflict refusal, zombie-twin rejection) now await restoreDrafterModel like every other exit; (3) beginDrafterModel wrapped in try/catch that calls clearDraftingState and rethrows, closing the orphaned-gate window; plan-mode.test.ts pins rewritten to assert absence across five files plus the try/catch structure, red-proven by reopening the orphan window (pin fails); full gate rc44b exit=0, 1521 pass / 133 files
- [x] FIX: LOW: plan-mode seededHint concatenation glitch — planNote ends "…than a regular draft." and is concatenated directly with "${label}: …" (extensions/loops/goal-list-queue.ts:783-793), producing "…regular draft.Goal drafting — deep planning: …" (missing separator) — fixed in v0.35.45: the join is now explicit — (planNote ? `${planNote} ` : "") + body; behavioral test in tests/plan-mode.test.ts drives the REAL /goal plan command with a seed via MockPi and asserts the notified hint contains 'regular draft. Goal drafting — deep planning:' with no glued /draft\.\w/ boundary, red-proven by restoring the direct concatenation (exact glitch text from the finding reproduced in the failure output); full gate rc45 exit=0, 1522 pass / 133 files
- [x] FIX: LOW: /glla agents --tail renders child-transcript lines through ctx.ui.notify without ANSI/control-character sanitization (formatTranscriptEntry truncates but never strips escapes, extensions/goal-agents-panel.ts:152-170 incl. the verbatim [raw] path; extensions/goal-commands.ts:2124-2133) — unlike every other external-text projection in the codebase; a hostile child transcript can emit terminal escape sequences; the candidate scan also synchronously reads up to 25 full transcript files on the main thread — fixed in v0.35.46: (1) formatTranscriptEntry now runs ALL output paths (both [raw] fallbacks and the [role] text path) through sanitizeDisplayText, matching the codebase's other external-text projections; (2) the candidate scan no longer reads up to 25 FULL transcripts synchronously — the injected reader contract takes optional maxBytes and the production command passes a real partial tail read via fs.readSync from size-maxBytes with a 256 KiB window (TRANSCRIPT_SCAN_MAX_BYTES exported); the single matched file keeps a full read so the 'last N of M' detail stays honest; tests in tests/agents-panel.test.ts cover hostile-escape stripping on both paths and assert the scan requests the bounded window, each proven red by neutering its production edit; full gate rc46 exit=0, 1524 pass / 133 files
- [x] FIX: LOW: completions/handler parity gaps with no pinning test: /list add|import|rm handled (extensions/goal-commands.ts:1237-1255) but absent from /list completions; /loop resume|refine|polish handled (extensions/goal-loop.ts:898,1040) but absent from /loop completions (command-registration-collisions.test.ts only scans top-level command names) — fixed in v0.35.47: all seven verbs added to their getArgumentCompletions tables (/list add|import|rm plus pause, which the generic pin caught beyond the finding; /loop resume|refine|polish) with accurate descriptions; new parity pin in tests/command-registration-collisions.test.ts scans every `sub === "x"` dispatch literal inside cmdList/cmdLoop against the actual completion tables and fails on any handled-but-uncompleted verb (red-proven by dropping the rm entry — pin names it); full gate rc47b exit=0, 1525 pass / 133 files (rc47 exit=1 was the known auditor-process streaming flake: green solo, clean gate rerun)
- [x] FIX: LOW: overdueWaitBackstop mutates durable state (parked → active, clears pauseResumeAt) without checking extensionApiStale/sessionHandoffPending/mainModelRecoveryActive gates (extensions/goal-heartbeat.ts:216-257,650-653) — during a latched-stale heartbeat a durably parked wait becomes an ACTIVE goal with no dispatch until a fresh session_start, breaking the paused-is-safe invariant — fixed in v0.35.48: overdueWaitBackstop now gates BEFORE the lastOverdueWaitKey latch (skipped windows stay retriable): refuses flags.sessionHandoffPending/staleTerminalDone/extensionApiStale windows outright, and under mainModelRecoveryActive releases ONLY recovery-routed waits (reason startsWith 'main model recovery' → probe route that re-parks with a fresh resumeAt) while unrelated agent-authored waits stay parked until recovery resolves; tests in tests/wait-overdue-backstop.test.ts: behavioral handoff-window pin (real session_shutdown → stays paused, no ledger fire), behavioral recovery-active pin (agent wait stays parked, nothing dispatched — red-proven), and a source pin asserting the gates precede the latch (red-proven by dropping the recovery clause: both the pin and the behavioral test fail); full gate rc48 exit=0, 1528 pass / 133 files
- [x] FIX: MEDIUM: README has no verb-semantics documentation — the user explicitly flagged (2026-08-23): what /goal|/list|/loop audit MEAN vs start vs the new plan verbs lives only in code comments; insertion points: primary table right after the quick-start block (README.md:64), fold into the Drafting-rules paragraph (~118-124), cross-link at "## Which loop?" (~207) — USER-REQUESTED ITEM — fixed in v0.35.38: new "What the verbs mean" table right after the quick-start command fence (README ~:93) tabulating draft/start/plan/audit across all three surfaces with the audit trio's distinct semantics (one-shot fix-in-pass goal v0.29.8, collect-then-drain list item v0.31.0, forever cadence loop v0.29.0) plus the verify distinction and one-initiative rule; drafting-rules paragraph (~:118) now names plan as the fourth depth; "Which loop?" (~:207) cross-links the table; includes the DECIDED /list plan file-arg semantics (prose only, file paths stay bulk import, mentioned files are research input); full gate rc38 exit=0, 1514 pass / 132 files
- [x] FIX: MEDIUM: README Files map stale on release day: prompts listing (README.md:693-697) shows 4 files but prompts/ contains 7 — missing prompts/goal-loop-plan.md + goal-loop-plan-loop.md shipped IN this version, plus pre-existing goal-loop-forever-metricless.md; the extensions/*.ts listing is likewise far partial (~20 of ~38, no goal-agents-panel.ts, quota-retry.ts, settings-menu.ts, loops/goal-session.ts…) while reading as complete — fixed in v0.35.39 (README commit 1b371176): the Files map now enumerates EVERY file — 34 extensions/ + 10 loops/ + 7 prompts/ + 4 scripts/ — grouped by concern with one-line descriptions verified against each module's exports; a scripted cross-check confirmed zero missing entries vs disk at write time; includes both plan-draft prompts, goal-loop-forever-metricless.md, goal-agents-panel.ts, quota-retry.ts, settings-menu.ts, loops/goal-session.ts and all other previously omitted files; full gate rc39b exit=0, 1514 pass / 132 files
- [x] FIX: MEDIUM: commit 28131527's audit-kind exemption in applyMeasurement (both the metricHasMoved carve-out and the never-moved-stop guard) ships with ZERO regression pin — no test constructs a kind:"audit" loop through applyMeasurement; deleting either guard would pass the suite while silently changing audit-loop stall accounting that DESIGN.md promises stays verbatim — fixed in v0.35.40: two twin-loop regression tests in tests/loop-forever.test.ts pin both halves — identical flat-metric shapes diverge by kind (audit stalls from iteration 1 → plateau at window; non-audit pre-movement flats stay free), and a dead metric gets 'metric never moved' on plain loops but never on audit loops (final verdict stays plateau). Red/green proven by deleting both metricHasMoved carve-outs (both twins fail) and textually deleting the never-moved kind-guard (source pin fails); the never-moved guard is additionally unreachable-by-construction for audits since plateau always returns first, hence its source-level pin per DESIGN.md's verbatim promise; full gate rc40 exit=0, 1516 pass / 132 files

## DECIDE findings (2026-08-23 pass)

- [x] DECIDED: keep /list plan as the LONG DRAFT — no direct import path (user 2026-08-23: "we dont do file if we added a file then it would just read it but we are still doing a long draft") — when a file arg IS given, the drafting agent should READ the file as research input inside the extended interview (never bulk-import); the README verb-semantics finding covers documenting this

## Docs currency (fold into fixes above)

- docs/DESIGN.md consolidated addendum stops at v0.35.31 (line ~578) — plan mode's second drafting depth (DraftingDepth/draftingTemplateFile/clearDraftingState reset) is an architectural shape the doc's own convention should record; docs/INDEX.md recent-work trail (line ~16) likewise ends at v0.35.31 with no plan-mode mention (covered by the README Files-map fix item's docs sweep)

## Severity breakdown (2026-08-23 pass)

- HIGH (1 FIX): never-moved stop not resumable
- MEDIUM (7 FIX): lastOutcome persistence; seed-trust Done-when defeat; newObjective laundering; recovery notice never delivered; README verb-semantics (user request); README Files map stale; audit-kind exemption untested
- LOW (7 FIX): loop-stop announcement gaps ×1; refine-era improved flags; spec_item_progress minting; draftingDepth dead wiring; seededHint separator; --tail sanitization; completions parity; backstop staleness gates
- DECIDE (1): /list plan file-arg asymmetry
- (count correction: the 2026-08-23 breakdown lists EIGHT LOW FIX items; the 'LOW (7)' label above undercounts by one)

---
# Full-project audit pass — 2026-08-25 (v0.35.63, branch main)

Three parallel Explore surveys covered the requested music/browser scope,
core runtime/lifecycle, and tests/release/docs. Findings below are new
relative to the append-only history above; no existing finding was re-reported.

## NEW FINDINGS

- [x] DECIDED: defer the music/browser UI scope for this repository — the user confirmed that goal belonged elsewhere (2026-08-25)
- [x] FIX: MEDIUM: GitHub Actions grants `id-token: write` to the quality job that runs repository-controlled install/tests, expanding publish-token exposure beyond the publish job (.github/workflows/publish.yml:9-40) — fixed in 4658b402 (OIDC permission is scoped to publish; release-contract regression added in 07ce2d1d)
- [x] FIX: MEDIUM: auditor-extension allowlist validation is opt-in and fail-open, so CI/release can pass without checking the intended extension boundary (tests/auditor-extensions.test.ts:191; scripts/verify-auditor-extensions-offline.mjs:21-24; package.json:52-54) — fixed in 0f9fe373, b6336c9e, 07ce2d1d (mandatory hermetic fixture, isolated offline spawn, release-gate script, and regression coverage)
- [x] DEFERRED: real-host smoke coverage policy remains outside the release gate; no integration job was requested in this pass (2026-08-25)
- [x] FIX: LOW: CHANGELOG.md contains duplicate `## 0.35.35` release headings, making version-based release-note extraction ambiguous (CHANGELOG.md:517,531) — fixed in b6336c9e, 07ce2d1d (topic demoted under the single release heading and uniqueness pinned)

## SURVEY NOTES

The requested music tab/search/filter/collapsible-sidebar surface was not found:
this repository is a terminal Pi goal/list/loop/audit plugin, not the browser
music application. The existing `Music` reference is a historical fixture/path
prune and does not establish a music UI implementation surface. Runtime and
lifecycle surveys found no additional confirmed new FIX or DECIDE findings.

---
# Follow-up to the 2026-08-25 stall audit disapproval — v0.35.65

- [x] FIX: HIGH: the frozen-child escalation was initially wired only to a
  mock manager `abort` seam; the pinned pi-subagents root registry exposes
  polling but its production root session already provides
  `subagents:rpc:stop`. GLLA now binds that RPC to the admitted MAIN host
  generation, validates readiness/current top-level ownership, records
  requested/unavailable/failed outcomes, and ignores stale replies. The
  production path is exercised by the unmodified AgentManager plus its real
  RPC handler and a deterministic pending faux provider in
  `tests/subagent-stop-rpc.integration.test.mjs`; the old-generation,
  unavailable, and one-shot regressions remain in
  `tests/subagent-hang-detection.test.ts`. No upstream package source was
  changed.

---
# Follow-up to Screenshot_20260825_173552 — repair/replan deadlock

- [x] FIX: HIGH: activating a queued repair card copied `repairTarget` onto an
  ACTIVE synthetic goal, but `guardGoalBeforeContinuation` rejected every
  continuation because it required a confirmed `propose_task_list` redraft.
  The redraft tool was therefore unreachable: the card displayed REPLAN
  REQUIRED while its first turn never started. The guard now permits exactly
  one durable bootstrap continuation, records `replanPromptedAt`, includes the
  preserved target and concrete tool/confirmation step, blocks automatic
  repeats, and lets explicit `/list resume` re-arm one retry. Focused
  activation/first-turn/repeat tests and display coverage were added.

---
# Fresh full-project audit pass — 2026-08-27 (v0.35.71, branch main)

Four parallel read-only Explore surveys covered lifecycle/state, orchestration/tools, UI/settings/docs, and tests/release boundaries. Reports were cross-checked against the current tree; only confirmed findings absent from the append-only history above are recorded here.

## NEW FINDINGS

- [x] FIX: HIGH: goal markdown is written before the state ledger with no crash-recovery transaction; a process death or append failure between `writeGoalMd` and `persistStateLine` leaves the newer status/objective only in markdown, while `readState` replays the older ledger and never rehydrates that goal record (extensions/loops/goal-orchestrator.ts:685-755; extensions/goal-state.ts:72-89,1278-1313) — a paused/updated goal can resurrect with stale status or lose task/usage changes after restart. — fixed in 1e8e5033
- [x] FIX: HIGH: session ownership does not fence competing pi processes sharing a working-directory root; `session-owner.json` and `owner.json` are overwritten by the new PID, but a different PID is not checked as live/foreign and no exclusive lock/refusal exists (extensions/loops/goal-session.ts:525-565,931-986; extensions/loops/goal-activation.ts:1089-1135) — two hosts can dispatch and persist against the same state, with last-writer-wins objective loss. — fixed in 5b2b400f, 7ad6200f, 790063c9, d87c61ef
- [x] FIX: MEDIUM: queue hydration treats a sidecar with an already-known id as authoritative only in memory and never reconciles newer sidecar fields such as objective, repairTarget, parentId, or queue order (extensions/goal-commands.ts:915-929; extensions/goal-loop-core.ts:1168-1215) — after a torn state append, durable repair metadata or an updated objective can be silently discarded on reload. — fixed in 88ae0f10
- [x] FIX: MEDIUM: `writeQueueItemFile` reports an existing id collision as `{wrote:false}` without `failed`, and enqueue callers test only `failed` before committing the new item to state (extensions/goal-loop-core.ts:1089-1121; extensions/loops/goal-tools.ts:1746-1770) — the old sidecar remains and later recovery can load a different objective than the state entry. — fixed in 88ae0f10
- [x] FIX: MEDIUM: `/goal pause` and the length/stall continuation send paths do not share one pause fence; `scheduleContinuation` checks the supervisor pause but `cmdPause`, `sendStallEscalation`, and `sendLengthContinue` can run after the pause while an in-flight turn settles (extensions/goal-commands.ts:334-357; extensions/goal-continuation.ts:1080-1170; extensions/loops/goal-activation.ts:1860-1870) — explicit `/glla pause` can still enqueue automatic model activity. — fixed in d91e9725
- [x] FIX: MEDIUM: low-level `agent_start`/`turn_start`/`message_update`/`agent_end` activity acknowledges any same-owner dispatch, while only `before_agent_start` validates the exact marker (extensions/goal-continuation.ts:607-654; extensions/loops/goal-activation.ts:2303-2380) — an unrelated manual turn can clear a continuation watchdog and suppress recovery for the accepted dispatch. — fixed in d91e9725
- [x] FIX: HIGH: a transient failure of mechanical contract command 1 that passes its one retry returns success immediately, so commands 2 and later never run (extensions/goal-loop-shield.ts:206-287) — a completion can pass pre-audit without executing the full verification contract. — fixed in 91b26614
- [x] FIX: HIGH: `propose_loop_draft` and `propose_loop_refine` execute measure probes without the normal `MEASURE_TIMEOUT_MS` bound (extensions/loops/goal-tools.ts:1860-1863,1977-1982; extensions/goal-loop.ts:228-234) — a stuck or long-running measure command can hang the interactive/orchestrator turn indefinitely. — fixed in 3496f43b
- [x] FIX: MEDIUM: runtime loop measurement parses numeric stdout without checking the measure command's exit status (extensions/goal-loop.ts:228-234) — a failed command that prints a number can be accepted as a valid metric and falsely improve, regress, or prolong the loop. — fixed in 3496f43b
- [x] FIX: HIGH: hierarchical `/list show` and `list_status` labels collapse children to `1.1`/`1.2` while `/list next`, `list_activate`, and `/list remove` index the raw flat queue (extensions/loops/goal-tools.ts:2132-2160; extensions/goal-commands.ts:1225-1408) — visible item numbers can activate or remove the wrong work. — fixed in a51c03af, ac492a81, dc954431
- [x] FIX: HIGH: activation ignores a failed queue-sidecar deletion and proceeds after removing the item from memory (extensions/goal-loop-core.ts:1123-1129; extensions/loops/goal-list-queue.ts:739) — a reload can recover the leftover sidecar and resurrect a completed/activated item. — fixed in 837fc4d9
- [x] FIX: MEDIUM: post-audit `proposeGoal` ignores the boolean returned by `safeSteerUser` and reports success whenever no exception is thrown (extensions/loops/goal-auditor-hooks.ts:1313-1326) — stale or handoff contexts can claim a follow-up was delivered when it never reached the session. — fixed in 837fc4d9
- [x] FIX: MEDIUM: managed subagent ownership is detected by an unanchored body substring rather than an exact frontmatter field (extensions/goal-loop-subagents.ts:211-214) — a user-owned agent file quoting the marker in its prompt can be overwritten or deleted. — fixed in 837fc4d9
- [x] FIX: HIGH: stale command contexts can mutate durable `/goal` and `/loop` state because pause/cancel/tweak and loop dispatch do not perform the same entry admission fence as creation/resume (extensions/loops/goal-activation.ts:705-708; extensions/goal-commands.ts:131-194,334-357,529-548,658-851; extensions/goal-loop.ts:930-1209) — an old process can abort, tweak, archive, or overwrite work after a successor owns the root. — fixed in 8ba75198, d87c61ef, ac492a81
- [x] FIX: HIGH: an interactive settings dialog checks staleness only before awaiting the menu; its post-confirm handlers save settings without rechecking session admission (extensions/goal-commands.ts:2264-2272; extensions/loops/goal-settings-ui.ts:583-603,832-849) — an old dialog can overwrite configuration after session replacement. — fixed in fc643cfb
- [x] FIX: MEDIUM: `autoResume` is documented and surfaced as global-only but is omitted from `GLOBAL_ONLY_KEYS`, while startup reads only global settings and does not apply the aggressive-mode effective default (extensions/goal-settings.ts:225-236,372-389,447-453; extensions/loops/goal-activation.ts:1414-1423; extensions/settings-menu.ts:168-209; extensions/goal-loop-core.ts:2103-2117) — project settings/provenance or the Aggressive-mode row can promise auto-resume that cold restore will not perform. — fixed in fc643cfb, 931cc008, 4bf414f0, 0257ded0
- [x] FIX: LOW: docs/INDEX.md's recent-work trail stops at v0.35.64 although package.json and CHANGELOG.md are at v0.35.71 (docs/INDEX.md:14-23; package.json:3; CHANGELOG.md:3) — maintainers following the index miss the shipped worker, cadence, and retry changes. — fixed in cda9d0f1
- [x] DECIDED: retain the project-scoped `notifyCmd` trusted-repository hook with explicit disclosure that GLLA is a long-running objective power tool and the configured command may execute on lifecycle events (2026-08-27) — do not impose an arbitrary sandbox or duration limitation; users must treat an opened repository and its `.pi-glla/settings.json` as trusted.
- [x] DEFERRED: selected auditor extensions execute arbitrary extension initialization code in the detached worker despite the isolated-auditor framing (extensions/loops/goal-settings-ui.ts:1186-1249; extensions/auditor-extensions.ts:125-153,249-286; extensions/goal-loop-auditor-process.ts:866-899) — no confirmation or warning-policy change in this pass; revisit the trusted-code choice in a separate scoped goal (2026-08-27).
- [x] FIX: MEDIUM: the release contract hardcodes a `0.35.35` changelog heading instead of deriving the current version from package.json (tests/release-contract.test.ts:64-67; package.json:3; CHANGELOG.md:3) — a missing current-version release heading can pass `release:check`. — fixed in 0ded98d6
- [x] FIX: MEDIUM: README claims the npm package contains the full test suite, but the package `files` allowlist omits `tests/` (README.md:440-443; package.json:32-46) — published documentation contradicts the artifact contents. — fixed in 0ded98d6
- [x] FIX: MEDIUM: project-relative auditor extension specs are resolved against `~/.pi/agent` for every allowlist entry, not the project settings base (extensions/auditor-extensions.ts:157-166) — a valid project-local relative extension is silently dropped at dispatch. — fixed in 1a0d9d64, c21701c8
- [x] FIX: MEDIUM: the offline auditor-extension release check shells out through the POSIX `timeout` utility, which is not the GNU timeout wrapper on a vanilla Windows install (scripts/verify-auditor-extensions-offline.mjs:31-34; tests/auditor-extensions.test.ts:200-204) — the release gate is not portable despite Windows launcher support. — fixed in 32874635, aed6f0e2
- [x] FIX: LOW: scripts/smoke.sh's `wait_for` searches the entire captured pane with unanchored regex `grep -q`, so broad markers such as `?`, `Yes`, or `✓ done:` can be satisfied by model prose before the intended UI transition (scripts/smoke.sh:132-139) — the live smoke harness can report success or send the next keystroke at the wrong state. — fixed in 8a301318, d0765f6d

---
# Full-project follow-up tasklist — 2026-08-31 (v0.36.0, branch main)

Fresh verification followed the fork-storm containment work. The release gate
is green (1,763 pass, 1 skip, 0 fail), `npm audit --omit=dev` reports zero
vulnerabilities, and the previously deferred ownership fence and generic wait
backstop are already implemented in the current tree. Only the distinct,
still-reproducible residual work below is reopened.

## OPEN FINDINGS

- [x] FIX: HIGH: terminal archival is still not crash-transactional — `archiveCurrentGoal` publishes the exclusive archive and deletes the active markdown before it persists the terminal/empty live slot (extensions/loops/goal-orchestrator.ts:1126-1199); a crash or failed state append in that window reloads the old ACTIVE ledger state beside an existing archive, and the next archive attempt is permanently fenced by that archive. Add a durable archive intent/commit journal plus startup reconciliation, with kill-point tests before/after archive publication, state persistence, and active-file cleanup. — fixed in v0.36.1 with archive-intent recovery and persistence-recovery regressions
- [x] FIX: HIGH: destructive queue paths still discard RAM/ledger state after queue-sidecar deletion fails — `/list clear`, `/list cancel`, and `/list remove` clear/remove entries despite failed or unchecked unlink results, and group-close/repair-source consumption also ignore deletion failure (extensions/goal-commands.ts:1408-1464,1528-1544; extensions/loops/goal-orchestrator.ts:1238-1249; extensions/loops/goal-tools.ts:2790-2799); the surviving sidecars are rehydrated after reload, undoing explicit user deletion/cancellation. Make every path fail closed or land a durable tombstone before state mutation, and inject unlink/readdir failures in regressions for all five paths. — fixed in v0.36.1 with fail-closed deletion results and queue recovery regressions
- [x] FIX: MEDIUM: the active ledger has no bounded rotation/segment policy — this repo's `active.jsonl` is already 29,379,138 bytes / 10,982 events and an isolated `readState` takes about 123 ms because it must scan every historical state record (extensions/goal-loop-core.ts:1413-1435). Add ownership-fenced, crash-safe compaction that preserves one complete current-state projection plus a bounded forensic event tail while retaining older history in immutable archive segments; prove recovery across every rename/append interruption point. — fixed in v0.36.1 with ownership-fenced segments and rename-window recovery tests
- [x] FIX: MEDIUM: several user-facing/runtime readers still synchronously materialize and parse the entire active ledger on the main process — `/list depth`, `/glla log`, `/glla switchlog`, postaudit reviewer cadence, and multi-project stats use `readFileSync` plus `parseLedgerEntries` (extensions/goal-commands.ts:1236-1241,1891-1934; extensions/loops/goal-auditor-hooks.ts:1794-1805; extensions/goal-loop-stats.ts:147-155). Reuse a shared chunked forward/reverse scanner or streaming reducer so bounded tails stop early and aggregate views retain exact semantics without whole-file allocations. — fixed in v0.36.1 with shared bounded scanners and streaming reducers
- [x] FIX: LOW: detached-auditor scratch has no project-wide stale-directory collector — normal attempts remove only their own directory and restart reaping scans only names matching the current logical attempt (extensions/goal-loop-auditor-process.ts:683-749,1132-1138,1473-1479); this repo still has 98 orphaned `audit-jobs` directories (3.2 MiB). Add a read-only health report and age-bounded cleanup that deletes only directories whose worker identity is proven dead, preserving unreadable/ambiguous locks. — fixed in v0.36.1 with health inventory and proven-dead-only cleanup
- [x] FIX: LOW: the extracted runtime bridge defeats TypeScript at the highest-risk lifecycle boundary — `extensions/loops/goal-runtime-globals.ts` declares more than 200 globals as `any`, contributing to 483 `any`-shape matches under `extensions/`, so renamed or shape-drifted session/auditor/queue wiring can typecheck and fail only at runtime. Replace the ambient bag with a typed runtime interface (incrementally by subsystem) and add a compile-time completeness check for every registered global. — fixed in v0.36.1 with a typed registration registry and completeness regression
- [x] FIX: LOW: `release:check` validates `npm pack --dry-run` but never imports the actual packed artifact; source-tree tests can pass while a required runtime file is absent or an export works only through undeclared local layout. Add a temporary-directory pack/extract/install smoke that loads `extensions/loops/goal.ts` with optional providers absent and verifies the packaged auditor worker/launcher paths resolve, without publishing or mutating the working tree. — fixed in v0.36.1 with temporary install/import smoke

---
# Fresh cwd-scoped project audit pass — 2026-09-01 (post-v0.36.1 working tree)

Three parallel read-only scouts surveyed lifecycle/recovery, UI/subagent
visibility, and audit/release/documentation boundaries. Findings below are
new relative to this append-only history; no existing finding was re-reported.

## NEW FINDINGS

- [x] FIX: MEDIUM: recovery-resume, session-handoff, and pending-list sidecars accept future-dated timestamps because freshness checks only require `Date.now() - at < window` (`extensions/goal-recovery.ts:63`; `extensions/loops/goal-session.ts:796,860,915`) — a future marker can be consumed as fresh and trigger an unexpected resume or replay. Require a non-negative age and add regressions for every sidecar validator. — fixed in 265dafa6 (shared non-negative-age gate + 3 regression tests)
- [x] FIX: MEDIUM: `/glla agents --tail` only identity-scans the newest 25 session transcripts (`extensions/goal-agents-panel.ts:246-282`) — a tracked child whose matching JSONL is older than that cap is reported as missing even though its transcript exists. Preserve bounded I/O without silently hiding tracked children and add a position-26 regression. — fixed in d747edac + e1702fa0 + 37f28e2 (session-id direct lookup, propagated runtime identity, 16-panel-test suite)
- [x] FIX: HIGH: list-audit fan-out reads `${cwd}/.pi-glla/audit-loop/findings.md` directly instead of the admitted configured state root (`extensions/loops/goal-orchestrator.ts:983-990`; `extensions/goal-loop-forever.ts:589-601`) — with `stateRoot=sessionDir`, collected findings are counted from one root but never queued from the other. Route the reader through the selected root and add a sessionDir regression. — fixed in d46449e (selected-root reader + behavioral sessionDir regression)
- [x] FIX: LOW: `docs/INDEX.md` promises “manual install / symlink setup” while `INSTALL.md` documents npm/source invocation but no symlink procedure (`docs/INDEX.md:32-39`; `INSTALL.md`) — package-store and contributor readers cannot follow the indexed path. Align the wording or add the supported symlink steps, and review the shipped README/install docs before release. — fixed in ca5e597 (aligned source-install wording, refreshed README/changelog, pack-verified shipped docs)
- [x] FIX: MEDIUM: Widget task-linkage header mislabeled as agent row — fixed in edc1d3b9 (splice skips `agent: ` prefix for `→ ` headers; regression in tests/display.test.ts) — header `→ <objective>` does not start with two spaces so splice prefixes `agent: `, rendering `├─ agent: → <objective>` (extensions/goal-loop-display.ts:1256)
- [x] FIX: MEDIUM: Status line has no width budget and can exceed terminal width — fixed in edc1d3b9 (buildStatusText takes width, cell-aware truncate via pi-tui; refreshUI passes terminal width; regression in tests/display.test.ts) — auditing/paused/loop branches concatenate meter, tally, badges, verdict age plus raw `extras.agents.line` with no truncation (extensions/goal-loop-display.ts:967)
- [x] FIX: MEDIUM: Settings TUI tabs row unbounded — fixed in 027bb6b3 (tabs join truncated ANSI-aware to width-2; render pin at 80 cols in tests/settings-menu-complete.test.ts) — 8 `label (count)` tabs joined with double spaces, ~110+ chars at width 80, never truncated while title and cells are (extensions/settings-menu.ts:828)
- [x] FIX: MEDIUM: `truncate()`/`wrap()` count JS chars not terminal cells — fixed in 027bb6b3 (truncateCells code-point walk + wrap packs by visibleWidth with column-based hard split; pi-tui truncateToWidth rejected — emits ANSI resets into plain text; regressions in tests/display.test.ts) — CJK/emoji objectives overrun budgets; widget outer truncation masks it but status line and `wrap` line-breaks do not (extensions/goal-loop-display.ts:60)
- [x] FIX: MEDIUM: Doc-promised `└ blocks: <parent wait> (zombie stand-down)` row from `inFlightToolCalls` not rendered anywhere — fixed in 5e1fc8e7/bff998ff (AgentsPanelRow.blockedBy set at snapshot from observed in-flight subagent waits only; per-row correlation impossible so the wait NAME is named, never inferred; render + assembly regressions in tests/agents-panel.test.ts) — `AgentsPanelRow` has no blocking field (docs/DESIGN-subagent-visibility.md:46)
- [x] FIX: MEDIUM: Doc-promised `subagent_hang_detected` "Recent hangs" panel footer missing — fixed in 5e1fc8e7/bff998ff (cmdAgents feeds last 3 durable hang events into renderAgentsPanel; absent when empty, never a placeholder; render regression in tests/agents-panel.test.ts) — `renderAgentsPanel` ends at cap notice, no ledger footer (docs/DESIGN-subagent-visibility.md:33)
- [x] FIX: LOW: Doc row shape drift — doc shows single-line `● <type> <summary> RUNNING 4m12s tools…` but code renders two-line — fixed in bff998ff (doc corrected to the shipped v0.38.22 two-line shape with blocks + hangs examples; code pins kept) `type · summary` + `STATE · PHASE · elapsed` rows (docs/DESIGN-subagent-visibility.md:40)
- [x] FIX: LOW: `/glla agents --tail` ambiguous prefix silently picks first match — fixed (exact id resolves directly; 2+ prefix hits list candidates and refuse; regression in tests/agents-panel.test.ts) — `find(a.recordId === id || startsWith(id))` never warns on multiple hits (extensions/goal-commands.ts:2461)
- [x] FIX: LOW: Headless settings fallback rows unbounded — fixed in 027bb6b3 (flat option strings capped at 120 cells; resolution prefix sits at the head so tail truncation is safe) — flat `select` strings embed full VALUE + DESCRIPTION with no truncation (extensions/loops/goal-settings-ui.ts:740)
- [x] FIX: LOW: Stale file headers claim both loop UI modules are `extensions/loops/goal.ts` ("The goal loop") — wrong module identity for maintainers (extensions/loops/goal-ui.ts:1) — fixed (header names the real module + role)
- [x] FIX: LOW: Stale file headers claim both loop UI modules are `extensions/loops/goal.ts` ("The goal loop") — same wrong header duplicated (extensions/loops/goal-settings-ui.ts:1) — fixed (header names the real module + role)
- [x] FIX: LOW: `completionSummaryLines` default 240-char values emit ~250-char chat lines with no width option — fixed in 027bb6b3 (optional lineWidth param on completionSummaryLines + terminalCompletionSummaryLines; regression in tests/completion-summary-lines.test.ts); single-line projection is the only width-conscious path (extensions/completion-summary.ts:193)
- [x] FIX: LOW: Agents-panel `truncate`/`cleanField` can split surrogate pairs and miscount wide glyphs — fixed in 027bb6b3 (panel truncate delegates to shared truncateCells; covered by display cell-awareness regression) (`.slice` on JS length); panel/notify lines have no cell-width guard (extensions/goal-agents-panel.ts:88)
- [x] DECIDED (2026-09-06, user): Show count line — quiet hides only when zero tracked; tracked-but-healthy keeps the compact count line (HUNG still surfaces via ⚠). Implemented: removed the quiet+healthy early-return in `assembleAgentsExtras`, pinned in tests/subagent-display-richness.test.ts (extensions/goal-agents-panel.ts:190)
- [x] FIX: HIGH: looksLikePi substring match admits non-pi processes (`pip`/`pilot` match "pi") past the SIGTERM gate — fixed (argv-identity gate: argv[0] exact `pi`/`pi-*` or runtime+pi-script; `pip install pi-subagents`, `pilot`, `episode`, `capital` refused; pins in tests/state-root-owner.test.ts) — user-confirmed takeover then signals a pid that merely recycled into a matching name (extensions/state-root-owner.ts:110)
- [x] FIX: HIGH: model-supplied measureCmd test-run execs via bash -c before the Confirm dialog shows output; autoAcceptDrafts removes even the after-the-fact dialog (model-triggered shell exec) (extensions/loops/goal-tools.ts:2510) — fixed (informed-consent disclosure: draft + refine dialogs state the command already ran once; auto-accept notifies say the same; the pre-run itself is the documented show-real-numbers mechanism, now disclosed; source pins in tests/behavioral-orchestrator.test.ts)
- [x] FIX: HIGH: same pre-confirm shell exec on the refine path — newMeasure runs before user sees the re-baseline dialog (extensions/loops/goal-tools.ts:2627) — fixed (same disclosure treatment as the draft path; refine dialog labels the output 'already executed once by the orchestrator')
- [x] FIX: MEDIUM: scheduleContinuation clears abortedStandDown before actionable/guard checks, so no-op schedules discharge the user-abort latch (extensions/goal-continuation.ts:1064) — fixed (clear moved after actionable+guard gates; explicit resume paths already clear it themselves; order pinned in tests/stall-handling.test.ts)
- [x] FIX: MEDIUM: guard trusts durable-state absolute archivedPath for existsSync then discards the live goal (corrupted-state arbitrary-path probe + goal-nulling DoS) (extensions/goal-continuation.ts:938) — fixed (archivedPath honored only inside the archive dir; outside paths ledger faulty_objective_archive_path_anomaly and fall back to canonical; behavioral regressions in tests/faulty-objective-recovery.test.ts)
- [x] FIX: MEDIUM: 30s ownership-recheck throttle leaves up to 30s of competing writes after an owner change (extensions/loops/goal-session.ts:979) — fixed (throttle 30s→10s single JSON read; command entry already re-gates every invocation so interactive writes were never the exposure)
- [x] FIX: MEDIUM: last-wins supersede steals a live owner with no generation/epoch fence; racing mains flap the root (extensions/state-root-owner.ts:362) — fixed (same-session lineage reclaims quietly without steal event; anonymous `unknown-session` claimants refused + `owner_supersede_refused` ledger, explicit `/glla takeover` remains the path; regressions in tests/last-wins.test.ts)
- [x] FIX: MEDIUM: control-message predicate matches forgeable substrings, letting crafted text get legitimate messages projected out of context (extensions/context-checkpoint.ts:329) — fixed (dead `isGllaControlMessage` substring predicate deleted; it had zero callers and the live projection already keys on customType only; marker-text survival pinned in tests/context-checkpoint.test.ts)
- [x] FIX: MEDIUM: terminal completion notice has no generation fence and can fire into a successor session (extensions/goal-continuation.ts:1117) — fixed (`TerminalCompletionNotice.generation` stamped by the verdict-apply scope, sender refuses mismatch + `terminal_completion_notice_stale_generation` ledger; refusal doesn't consume fire-once; regressions in tests/terminal-completion-notice.test.ts)
- [x] FIX: LOW: proc start re-verified at classify time, not at signal time (pid recycled in the gap gets SIGTERMed) (extensions/state-root-owner.ts:280) — fixed (pre-signal re-read; changed/non-pi occupant refuses `claim-lost` + `owner_takeover_refused{reason:owner-changed}`, never signaled; regression in tests/state-root-owner.test.ts)
- [x] FIX: LOW: auto-advance silently skips head groups with no ledger entry (extensions/loops/goal-list-queue.ts:722) — fixed (`list_group_auto_skipped{groupId,skippedGroups,landedOn}` ledgered on the scan path; behavioral regression in tests/list-subtasks.test.ts)
- [x] FIX: LOW: CLK_TCK env var overrides the pid-reuse clock divisor and can skew the recycled verdict (extensions/state-root-owner.ts:80) — fixed (hardcoded 100 with safe-direction rationale: overestimates occupant age → fails toward refuse, never toward false recycled)
- [x] FIX: LOW: archive temp names use Math.random (predictable; wx-flag mitigated, use randomUUID) (extensions/loops/goal-orchestrator.ts:1178) — fixed (`node:crypto randomUUID` in the temp name)
- [x] FIX: LOW: globalThis bridge keeps ~200 names with an any-typed function remainder and no duplicate/overwrite detection (extensions/loops/goal-runtime-globals.ts:14) — fixed (globalThis-held per-name registration counts, last-write-wins preserved for reload-correctness; contract test asserts source-level no-dup + runtime exactly-once for every registry name; the function remainder stays explicitly typed as GoalRuntimeFunction by design)
- [x] DECIDED (2026-09-06, user): Harness-test + contract-check — VERIFIED LIVE, no move needed. pi's type contract (`MessageEndEventResult.message`, "Replace the finalized message") + runtime (`agent-session.js` `emitMessageEnd` → `_replaceMessageInPlace`, role-preserving) honor the return; existing harness tests in tests/stale-continuation-integration.test.ts drive `pi.handlers.get("message_end")` and pin the sanitized return incl. role/customType preservation (extensions/loops/goal-activation.ts:1029)
- [x] FIX: HIGH: headless /glla fallback omits compactorModel/compactorModelFallbacks + subagentDisplayRichness (file:extensions/goal-commands.ts:2695) — fixed (three rows added with provenance; source pins in tests/settings-editors.test.ts)
- [x] FIX: LOW: status-line width budget truncated the `/glla resume` action off LIST QUEUED status (found by the release gate: list-invisible-restart v0.35.61) — fixed (waitingListStatus budgets the objective against prefix/suffix so the action survives; width threaded through buildStatusTextBase) (extensions/goal-loop-display.ts)
- [x] FIX: LOW: list-invisible-restart tests leaked cold-restore auditor-surface suppression into co-resident files (3 display.test.ts failures in combo runs) — fixed (afterEach `__testOnlyResetAuditorSurface`; pre-existing isolation gap, not a product bug) (tests/list-invisible-restart.test.ts)
- [x] FIX: MEDIUM: normalizeLoadedSettings leaves tokenLimit/auditCap/stuck/stall/wedge/carryover/decisionPopup/aggressive/notifyCmd/auditorModel unvalidated for hand edits — fixed in df027a31 (junk numerics/enums/strings reset to unset so consumer `??` fallbacks apply; booleans reset to unset = default-on) + 1a9e1d1 (normalize junk-reset + valid-survive pins in tests/settings-menu-complete.test.ts) (file:extensions/goal-settings.ts:390)
- [x] FIX: MEDIUM: ModelSelector.retryDelayMs hardcodes 15m base and ignores scope/setting — fixed in 2bd7df49 (optional `retryBaseMinutes` dep, junk falls back to 15; main-session scope wires `mainModelRetryMinutes`) + 1a9e1d1 (base-honored + junk-base pins in tests/model-selector.test.ts) (file:extensions/model-selector.ts:124)
- [x] FIX: MEDIUM: drafter session-last-resort bypasses forbiddenModels gate — fixed in b21e0796 (last resort refused + `forbidden_model_switch` ledgered when the session model is forbidden; caller already handles empty selection) + regression in tests/designer-drafter-policy.test.ts (file:extensions/drafter-model.ts:88)
- [x] FIX: MEDIUM: compactor one-shot is process-local so restart refires brief/page per episode — fixed in b21e0796 (durable `compactor-fired.json` marker: restart mid-episode stays silent, recovery removes it to re-arm) + restart/re-arm regression in tests/compactor-handoff.test.ts (file:extensions/goal-compactor.ts:62)
- [x] FIX: MEDIUM: legacy reviewer key is readable but has no row/editor/migration so stale config is immortal — fixed in 2bd7df49 (`migrateLegacySettings` moves `reviewer` → `postaudit` when postaudit absent, deletes the legacy key; postaudit wins) + 1a9e1d1 (migration + postaudit-wins pins in tests/settings-menu-complete.test.ts) (file:extensions/goal-settings.ts:589)
- [x] FIX: MEDIUM: auditorExtensionArgs emits raw specs while dispatch resolves fail-closed, so saved npm: entries silently vanish offline — fixed in b21e0796 (`auditorExtensionArgs` now resolves fail-closed via `resolveAuditorAllowedExtensions` before emitting `--extension` args; raw npm: dropped) + drop pin in tests/auditor-extensions.test.ts (file:extensions/auditor-extensions.ts:298)
- [x] FIX: LOW: allowlist normalize dedups case-sensitively while fallback chains dedup case-insensitively — fixed in b21e0796 (dedup key is `spec.toLowerCase()`, first spelling wins, original preserved) + pin in tests/auditor-extensions.test.ts (file:extensions/auditor-extensions.ts:265)
- [x] FIX: LOW: MultiModelPicker silently slices initialSelected to maxSelections with no omitted notice — fixed in b21e0796/6cf0ac02 (`initialOmitted` tail rendered as a width-safe warning line naming the omitted refs) + render pin in tests/multi-model-picker.test.ts (file:extensions/multi-model-picker.ts:120)
- [x] FIX: LOW: compactor worker kill is SIGTERM-only with no SIGKILL fallback — fixed in 6cf0ac02 (SIGTERM then `COMPACTOR_KILL_GRACE_MS=5s` SIGKILL when still alive; exit-aware) + source pin in tests/compactor-handoff.test.ts (file:extensions/goal-compactor.ts:155)
- [x] FIX: LOW: release smoke jiti alias points at nonexistent @tintinweb/pi-subagents path (nothing imports the scoped name; package depends on unscoped pi-subagents) — stale tintinweb-era drift — fixed in 6cf0ac02 (stale alias removed with explanatory comment; no replacement — no extension imports pi-subagents directly) (file:scripts/release-pack-smoke.mjs:81)
- [x] FIX: LOW: no docs/SETTINGS*.md canonical reference; settings truth lives only in menu/headless/CHANGELOG — fixed in 6cf0ac02 (new docs/SETTINGS.md: files/precedence, global-only list, all 47 keys with defaults; indexed in docs/INDEX.md; drift pin in tests/settings-editors.test.ts asserts every SETTINGS_KEYS entry appears) (file:docs/INDEX.md:1)
- [x] FIX: MEDIUM: worker rows say "silent 0s" while the child is visibly working — fixed in 62046323 (activeAgeLabel: fresh `active {age}` / stale `quiet {age}`, wired into rows + detail; pins in tests/agents-panel.test.ts) — field 2026-09-07 screenshots: GLLA rows read `silent 0s` as a state claim while the native panel directly above shows the same scouts `active now · 5 turns · 12 tools`. The noun "silent" always reads as an accusation; fix by renaming the age label (fresh: `active {age}`, stale: `quiet {age}`) in renderAgentsWidgetLine + renderAgentsWidgetLines + panel detail line; head `stream {age}` stays (file:extensions/goal-agents-panel.ts:193)
- [x] DECIDED: head owns liveness (2026-09-07) — user chose head-keeps-lifesign; status drops its [LIVE/WORKING] capsule + last-stream tail, keeps state + counts
- [x] DECIDED: exceptions-only by default (2026-09-07) — user chose exceptions-only; richness default flips so healthy fan-out lives on the fleet panel, troubled rows + head readout stay, setting remains for opt-in detail
- [x] DECIDED: exceptions-only by default (2026-09-07) — user chose exceptions-only over head-readout-only/keep: richness default flips so healthy fan-out lives on the fleet panel, troubled rows + head readout stay, setting remains for opt-in detail
- [x] FIX: HIGH: compact count line flags hung only when the stalest child is hung — fixed (trouble flag scans all live children via hasHungWorker; aborting wording kept; sibling-scan pinned in tests/subagent-display-richness.test.ts), so a hung/aborting child hides behind a healthy stalest sibling, violating HUNG-never-silent (file:extensions/goal-agents-panel.ts:239)
- [x] FIX: MEDIUM: hasHungWorker covers hung + abort-requested but not action failed/unavailable — fixed (failed/unavailable fire, matching lifesignBandFor; ended-with-failure still excluded; pins in tests/subagent-display-richness.test.ts), though lifesignBandFor classifies both as hung and rich rows render red (file:extensions/goal-agents-panel.ts:201)
- [x] FIX: MEDIUM: headLifesign triangles only on status hung, ignoring action failed/unavailable — fixed (failed/unavailable triangle the head; pins in tests/lifesign.test.ts), so the head can breathe fresh/success while a sibling row renders error (file:extensions/goal-loop-core.ts:3739)
- [x] FIX: MEDIUM: head band uses raw freshestMs with no queued-cap and no abort handling — fixed (head band is now the worst per-row band via shared lifesignBandFor; queued caps, abort reads aging; readout stays freshest; pins in tests/lifesign.test.ts), so a queued-40m head reads stale/error vs row aging/warning and an aborting-10s head reads fresh/success vs row aging/warning, breaking one-snapshot-never-diverging (file:extensions/goal-loop-core.ts:3740)
- [x] FIX: LOW: fresh head age text paints dim, not the documented success ramp — fixed (fresh age rides success; pins in tests/display.test.ts via theme assertions) (head glyph + age text + row glyphs success <5m per CHANGELOG) (file:extensions/goal-loop-display.ts:1509)
- [x] FIX: MEDIUM: completedGoalLines emits `✓ done` twice — fixed (outcome leads once; recap budget counts all fixed adornments; exactly-once pinned in tests/display.test.ts) and objBudget omits the outer prefix (~8 cells), so the recap truncates early (file:extensions/goal-loop-display.ts:1909)
- [x] FIX: LOW: widget prefixes every worker row with `agent: ` — fixed (bare glyph-first Option-2 rows; header special-case removed as dead; pins in tests/display.test.ts + tests/agents-panel.test.ts), contradicting the documented bare glyph-first Option-2 shape and burning ~7 cells before narrow-terminal truncation (file:extensions/goal-loop-display.ts:1310)
- [x] FIX: MEDIUM: status suppresses the worker summary while auditing — fixed (summary rides every branch incl. auditing for HUNG-never-silent; widget rows splice before the auditor block so observations+footer stay contiguous; pins in tests/agents-panel.test.ts + tests/display.test.ts), yet the widget splices worker rows directly above the auditor footer, mixing the two surfaces the code claims stay separate (file:extensions/goal-loop-display.ts:1009)
- [x] FIX: LOW: head `total` elapsed ticks wall-clock every render — fixed (goalTotalText rides the shared 5s/15s/30s bucket grain; goal-ui comment states the honest contract; pins updated in tests/display.test.ts), so setWidget refires past the diff guard and churns the shared belowEditor stack order despite the genuine-transitions comment (file:extensions/loops/goal-ui.ts:807)
- [x] FIX: LOW: orphan-worker fallback invents an undocumented `● active workers` header — fixed (count line leads when present, else bare rows; pins in tests/agents-panel.test.ts) after v0.38.23 removed headers, duplicating the count-line role (file:extensions/goal-loop-display.ts:1324)
- [x] FIX: LOW: loop branchName line is appended after the `└─` footer — fixed (branch rides an ├─ detail row before the footer; footer-last invariant restored), breaking the footer-last tree invariant the worker-row splice relies on (file:extensions/goal-loop-display.ts:1954)
- [x] FIX: HIGH: sendContinuation omits abortedStandDown — fixed (send-side abort-latch check; explicit resumes clear the latch so no deadlock; source pins in tests/stall-handling.test.ts), so an armed timer fires after user-Esc/zombie abort (file:extensions/goal-continuation.ts:1231)
- [x] FIX: HIGH: sendStallEscalation omits abortedStandDown — fixed (same latch check; source pins in tests/stall-handling.test.ts), so a stall nudge resurrects a stood-down chain (file:extensions/goal-continuation.ts:1372)
- [x] FIX: HIGH: sendLengthContinue omits abortedStandDown — fixed (same latch check; source pins in tests/stall-handling.test.ts), so a length nudge resurrects a stood-down chain (file:extensions/goal-continuation.ts:1414)
- [x] FIX: HIGH: sendTerminalCompletionNotice omits stoodDown/pending/aborted checks — fixed, scoped (refuses on abortedStandDown with ledger entry; deliberately not on stoodDown/pending — closure wins there since a pending dispatch fences itself on the archived goal, and dropping the notice would lose transcript closure with no retry; source pin in tests/stall-handling.test.ts), so a done-notice double-queues behind a live dispatch or abort (file:extensions/goal-continuation.ts:1137)
- [x] FIX: MEDIUM: agent_start/turn_start fallback acks dispatch with no marker match — fixed (message_start stamps genuine user-role messages via noteUserMessageForDispatch; fallback refuses + ledgers continuation_start_ack_refused_user_turn when the stamp postdates the send; before_agent_start marker still acks; source pins in tests/stall-handling.test.ts)
- [x] FIX: HIGH: zombie-timer `goalId!==undefined||goal` routes loop aborts into the goal branch — fixed (retry routes on the abort closure goalId; a goal started during the delay no longer strands the loop paused forever; source pin in tests/stall-handling.test.ts), leaving the loop paused forever when any goal object exists (file:extensions/loops/goal-activation.ts:517)
- [x] FIX: MEDIUM: silent-successor absorb bumps generation without clearing pendingContinuationDispatch — fixed (shared clearDeadGenerationDispatch helper: clears the dead dispatch via clearContinuationStartWatchdog + clearDispatchRecord with a successor_absorb_cleared_stale_dispatch ledger, called from both absorb and self-heal paths, so the tail's fresh schedule fires; force schedule already releases stoodDown)
- [x] FIX: MEDIUM: writeOwnerFile non-atomic read-then-overwrite — fixed (compare-and-swap refresh: re-read before write, abort when the record now names a foreign pid; sole caller refreshes own claim so skipping is always safe)
- [x] FIX: MEDIUM: supersedeLiveOwnerRoot unlink-then-claim — fixed, bounded (compare-and-swap the unlink: re-read pid/at/instanceId before removeOwnerFile on both reclaim + steal paths, refuse + ledger owner_supersede_refused reason record-changed when the owner is actively heartbeating; a wedged unchanged record still supersedes; the residual async-notify window is inherent — the dethroned side stands down on its next recheck)
- [x] FIX: MEDIUM: 10s ownership-recheck throttle serves cached held/lost — fixed (refreshOwnershipStanding takes force; command entry warnIfStaleAtEntry forces a fresh read per command while background pollers keep the throttle)
- [x] FIX: HIGH: fallback-exhausted cycle-reset re-activates goal/loop immediately with no horizon/delay — fixed, scoped (each new cycle does attempts+1 so the next failure's envelope delay grows, and past the 24h horizon the reset holds for manual resume via holdMainModelRecovery instead of resurrecting a turn; the probe turn itself stays prompt BY DESIGN — the backoff wait is served between failure and probe, and v0.34.132's executable contract pins the synchronous resume because the supervised turn on current IS the health check; deferring it to a timer demonstrably stranded the probe with providerCalls stuck at 1 — source pins in tests/stall-handling.test.ts), so bounded recovery cycles forever (file:extensions/goal-recovery.ts:1332)
- [x] FIX: MEDIUM: session_start unconditionally clears mainModelAbortForRecovery/lastMainModelFailure — fixed (clear only when no recovery episode is active; parked recovery preserves the markers so post-restore abort settlement still fires the failover continuation and the loop path reads the durable failure; all settle paths clear both, no leak)
- [x] FIX: LOW: stale-continuation sanitizer exempts length-continue — fixed (exemption removed: marker-less length text with live supervision already falls through to delivered, so only the no-supervision stale case changes; old never-stale pin rewritten with the rationale; functional pins in tests/stale-continuation.test.ts; task-5 guard pin in tests/length-continue.test.ts updated for the abort latch)
- [x] FIX: MEDIUM: absorb/self-heal notify consumes the rearm latch with no continuation when old pending survives — fixed (same root cause as the absorb finding: orphaned pending blocked the tail schedule after a truthful-sounding rearm notify; the shared clearDeadGenerationDispatch helper in tryAbsorbHostSuccessor + selfHealStaleSameSession removes the orphan so the notify is truthful)
- [x] FIX: MEDIUM: stallShortWords editor promises `0 = off` but save persists 0 into a normalizer that deletes it — fixed at the root (normalizer keeps 0; editor already saved it correctly; consumer already honors 0 via wordCount < 0)
- [x] FIX: MEDIUM: normalizeLoadedSettings deletes stallShortWords <= 0 — fixed (`<= 0` → `< 0`; junk test updated: -1 still normalizes, 0 = off survives; pins in tests/settings-menu-complete.test.ts)
- [x] FIX: MEDIUM: consumer uses `s.stallShortWords ?? DEFAULT_STALL_SHORT_WORDS`, so the off-state is unrepresentable — no change needed (0 is not nullish so it flows through, and wordCount < 0 never fires: 0 already disables; verified, not assumed)
- [x] FIX: MEDIUM: auditorModel flow has no inherit/clear for thinking — fixed (ladder reuses drafterThinkingChoiceOptions with the session-inherit row; choosing it clears to undefined, which both spawn sites already read as inherit; pins in tests/model-picker.test.ts)
- [x] FIX: MEDIUM: picking a non-reasoning auditor model returns early without clearing stale auditorThinkingLevel — fixed (early-return branch clears the override + says so in the notify; pins in tests/model-picker.test.ts)
- [x] FIX: LOW: subagentDisplayRichness doc comment still advertises rich = rows + task-linkage header — fixed (the :248 type comment was already corrected under Task 1's exceptions-only change, daemon commit a2998a5c; the remaining stale twin in the DEFAULTS block at :358 rewritten by this task: quiet default, no linkage claim)
- [x] FIX: LOW: settings-menu row description still advertises `rich shows worker rows + task linkage` — already fixed under Task 1 (daemon commit a2998a5c rewrote it to the quiet-default wording; verified via git show)
- [x] FIX: LOW: picker option still advertises `rich — worker rows + task linkage (recommended)` — already fixed under Task 1 (daemon commit a2998a5c: `rich — all worker rows` + quiet default; verified via git show)
- [x] FIX: LOW: goal-ui comment still advertises the task-linkage header after its removal — already fixed under Task 1 (daemon commit a2998a5c touched goal-ui.ts with the exceptions-only comment; no linkage claim remains — verified via grep)
- [x] FIX: LOW: quiet comment vs count-line behavior — fixed (type comment now documents the count line staying at every level per the 2026-09-06 ambient-awareness decision; pin in tests/settings-menu-complete.test.ts)
- [x] FIX: LOW: menu quiet copy contradicts the count line — fixed (description now `quiet shows troubled workers only + the count line`; pin in tests/settings-menu-complete.test.ts)
- [x] FIX: LOW: picker quiet copy contradicts the count line — fixed (option now `quiet — troubled workers only + the count line`; pin in tests/settings-menu-complete.test.ts)
- [x] FIX: LOW: cmdReviewerSettings comment vs migration — fixed (comment now documents that reviewer-key writes don't stick: load-migration consolidates into postaudit; the write-to-read-key code is unchanged and accurately described)
- [x] FIX: LOW: SETTINGS_KEYS reviewer entry dead provenance — fixed (entry comment now documents the full lifecycle: menu saves can still write it when postaudit is unset, load-migration consolidates it, provenance must resolve it or values report unknown)
- [x] FIX: LOW: DESIGN-subagent-visibility two-line worker-row example — fixed (example rewritten to the shipped one-glyph-line rows with the footer-deletion + id-placement notes; orphaned old-example tail removed)
- [x] FIX: LOW: SETTINGS.md wedgeAlertMinutes vs aggressive default — fixed (row now reads `unset (30, or off while aggressive mode is on — the default)` + notes the menu shows effective; verified against resolveEffectiveAggressiveSettings: unset → aggressive ? 0 : 30, aggressiveMode !== false)
- [x] FIX: LOW: SETTINGS_MUTATING_ACTIONS treats bare `/glla fallbacks` as mutating — fixed (gate in goal-commands.ts is now arg-aware: bare display allowed on stale handle, clear/off/unset/none still refused + ledgered; core set comment documents the carve-out; behavioral + source pins in tests/glla-stale-context.test.ts)
- [x] FIX: LOW: notifyCmd `auto` display vs literal trap — fixed (editor maps auto/auto-detect/default/empty → unset so the visible word round-trips; menu `auto` stays truthful, headless `(unset)` stays accurate; behavioral pin in tests/settings-editors.test.ts)

---
# Full-project audit pass — 2026-09-08 (v0.38.29, branch main)

Three parallel scout surveys (lifecycle/recovery, auditor/display/commands/settings UI,
tests/docs/packaging/loop-forever), all collected; every finding below was re-verified
against the tree by the orchestrator before recording. Append-only; boxes checked only
after the fix commit lands. F2-indent counted once as DECIDE (normalize vs enforce-flat);
UI narrow-width/plain-text duplicates folded into their FIX lines.

- [x] FIX: LOW: approval-render replay runs before the stale/ownership fence (extensions/loops/goal-activation.ts:779-786,789-795,873-878 plus review/list wrappers call replayUndeliveredApprovalRenders before cmdGoal/cmdLoop/cmdList stale fence; replay rewrites pending-approval-renders.json deliveredAt + ledgers in extensions/approval-render-store.ts:117-145; /loop status explicitly allowed on stale handles in extensions/goal-loop.ts:971-974 yet wrapper replays first, so a superseded session marks delivery into a dead session) — fixed in 779ba31d (guard) + pins in 6e32bb05
- [x] FIX: LOW: countDone counts only direct children of closed parents while countTotal recurses all descendants (extensions/goal-loop-display.ts:1341-1356 adds 1 + subtasks.length; Task.subtasks recursive in extensions/goal-loop-core.ts:108; closed parent with nested grandchildren under-reads done, e.g. parent + 1 child + 5 grandchildren counts 2 done of 7 total; v0.38.27 port pins only shallow 7/12 + 1/4 shapes) — fixed in 338b87a7 + pins in 6e32bb05
- [x] FIX: LOW: plain-text surfaces still use the rejected ANSI-emitting truncator (extensions/goal-loop-display.ts:61-66 documents pi-tui truncateToWidth wraps ellipsis in ANSI resets even for plain text and switches display to truncateCells, but extensions/completion-summary.ts:2,306 and extensions/loops/goal-settings-ui.ts:25,745 still call truncateToWidth; chat notify / headless flat rows are plain-text consumers, truncated lines carry stray resets into logs/pagers) — fixed in 338b87a7 + pins in 6e32bb05
- [x] FIX: LOW: budgetFor floor defeats narrow widths and recovery truncates ignore width entirely (extensions/goal-loop-display.ts:262 Math.max(floor, width-margin-prefix); modelProvenanceLines :343 floor 60 returns 60-cell budget at width 40 so inner truncate no-ops and outer tuiTruncateToWidth hard-cuts mid-token; compactMainModelRecoveryLine :311,333,338 fixed 48/56 with no width param drops attempts/skipped tail at narrow widths) — fixed in 338b87a7 + pins in 6e32bb05
- [x] FIX: LOW: displayObjective strips only 3 Markdown wrappers while compactness doc claims broader strip (extensions/goal-loop-display.ts:94-99 removes **..**, __..__, `..` only; # Header, *em*, [label](url), > quote objectives keep litter on glance card; audit/UI-CARD-COMPACTNESS-2026-09-08.md claims Markdown wrappers stripped) — fixed in 338b87a7 + pins in 6e32bb05
- [x] FIX: LOW: approval-render store ledger spam + surrogate-unsafe slice + unbounded lines (extensions/approval-render-store.ts:47-68 corrupt-file read appends terminal_approval_render_store_invalid ledger every read without repairing the file; :85 objective.slice(0,300) char-slices surrogate pairs; chatLines has no per-line/byte cap behind the each-is-~1KB comment so a long trailer grows the 20-entry sidecar without bound) — fixed in 6352f525 + pins in 6e32bb05
- [x] FIX: LOW: goal card recovery/provenance block is kind-blind (extensions/goal-loop-display.ts:1652-1665 shows compact recovery when g.status active and any state.mainModelRecovery with no kind check, suppresses modelProvenanceLines whenever any recovery exists; kind loop episode beside an active goal misattributes loop recovery to goal card and hides goal provenance; blank-primary edge compactMainModelRecoveryLine :294 returns undefined while provenance stays suppressed, leaving no model fact at all; loop/standalone cards at :1480-1481 already own loop kinds) — fixed in 338b87a7 + pins in 6e32bb05
- [x] FIX: LOW: topOpenAuditFinding strip lags its own matcher (extensions/goal-loop-forever.ts:603 finder accepts aligned/tabbed boxes via /^- \[[ \t]+\]/ per 77a684ce but :604 strip only removes exact single-space boxes via replace(/^- \[ \]\s*/,""); a - [  ] box returns markup into the reprieve note injected into the next iteration prompt) — fixed in 338b87a7 + pins in 6e32bb05
- [x] FIX: LOW: parseLoopStartArgs eats known-key = words in unquoted prose even when the value is junk (extensions/goal-loop-forever.ts:421-436 kvRe matches (\w+)= anywhere, KNOWN_KEYS filter :422 plus unconditional spans.push :436 strips the span from the target while silently falling back to defaults; e.g. unquoted /loop start fix time=out handling loses time=out from the target with no warning; quoted targets protected per da1273d9, unquoted prose with time=/max=/tokens= is not) — fixed in 338b87a7 + pins in 6e32bb05
- [x] DECIDED: migrate-on-read for pre-v0.38.21 audit histories (2026-09-08) — user chose migrate-on-read over grandfather: backfill superseded flags on read so no stale retry ever argues settled objections; queue as follow-up work. — shipped in v0.38.32.
- [x] DECIDED: normalize findings.md indent convention (2026-09-08) — user chose normalize over enforce-flat: accept optional indent in countOpen/topOpen/auditMeasureCmd + fan-out together; queue as follow-up work. — shipped in v0.38.33.
- [x] FIX: LOW: overdue wait-pause claims imminent resume forever + generic tail (extensions/goal-loop-display.ts pausedNextTransition returns "resuming now" for any passed pauseResumeAt; card body "auto-retrying · now" and status bar "resuming…" likewise unbounded; wait card closes with stock provider-failure pauseSuggestedAction boilerplate + "awaiting first turn — resumes exactly here" identical on every recovery wait — field screenshot 20260908_180721: 1h-overdue retry on held host shows paused + safely parked + next: resuming now simultaneously) — fixed in 6a0cada0 (grace-bounded resume + wait-tail drop, 6 pins) + 631f7a70 (gate fallout pins updated); gate 2010 pass, shipped in v0.38.31

---
# Full-project audit pass — 2026-09-10 (v0.38.44, branch main)

Three parallel scout surveys (lifecycle/recovery/session, display/auditor/
commands, tests/docs/packaging), all collected; every finding below was
re-verified against the tree by the orchestrator before recording (six
scout claims were disposed as unproven/by-design with rationale in
audit/FRESH-AUDIT-PASS-2026-09-10.md). Append-only; boxes checked only
after the fix commit lands.

- [x] FIX: HIGH: refreshUpdateCheck spawn has no error listener (extensions/glla-update-check.ts:64; SpawnFn type cannot express one) — a failed npm spawn emits an async error event the outer try/catch cannot catch, crashing the host on any command contact while the cache is stale; child also never unref'd and stderr never drained — fixed in 558ebd71
- [x] FIX: HIGH: formatGllaVersion reports up to date on unknown manifest version (extensions/glla-version.ts:84) — compareVersions(latest, "unknown") yields NaN→0, so the exact damaged-manifest case staleUpdateNudge guards against prints "This session is up to date." — fixed in 558ebd71
- [x] FIX: HIGH: withoutStaleNext drops the recorded-facts fallback Next (extensions/completion-summary.ts:217) — STALE_NEXT_PATTERN /review/i matches buildRecordedFactsCompletionSummary's "Next: review the durable record at …" (:468), so fallback approval chats lose their only concrete next action despite the first-concrete-action-survives contract — fixed in 558ebd71
- [x] FIX: MEDIUM: future-dated sidecar forces per-contact spawn storm (extensions/glla-update-check.ts:37,62) — strict reader nulls future checkedAt so refresh treats it as a permanent miss and spawns npm view on every command contact while no nudge ever renders — fixed in 558ebd71
- [x] FIX: MEDIUM: refresh caches unvalidated npm stdout token (extensions/glla-update-check.ts:70) — last-whitespace-token cached with zero semver check, so registry noise poisons the sidecar to TTL expiry: status stays silent while /glla version prints Registry latest v-garbage — fixed in 558ebd71
- [x] FIX: MEDIUM: formatGllaVersion hand-rolled sidecar parse diverges from readUpdateCheck (extensions/glla-version.ts:75) — honors future-dated/checkedAt-less caches the status path rejects with no TTL, so /glla version and the status tail can disagree about the same file — fixed in 558ebd71
- [x] FIX: MEDIUM: approval-render goalId dedup blocks a fresh render after delivered history (extensions/approval-render-store.ts:93) — delivered entries are retained as capped history, so the exists-check matches them too; an archive-failure retry (goal_archive_failed_after_approval → resume → re-approve, same goalId) silently drops the second approval chat while the caller believes it queued — fixed in b1284c04
- [x] FIX: MEDIUM: updateCheckPath ignores the sessionDir state root (extensions/glla-version.ts:9) — hardcodes cwd/.pi-glla while piGlaDir (extensions/goal-loop-core.ts:1210, via resolveGllaStateDir) is root-aware, so sessionDir mode splits the update sidecar from every other state file — fixed in 558ebd71
- [x] FIX: LOW: dead readUpdateCheck import (extensions/goal-commands.ts:39) — cmdGllaVersion goes through formatGllaVersion's own read; compiles only because noUnusedLocals is off — fixed in 71b68b86
- [x] FIX: LOW: auditorVerdictTally void now plus future lastAt renders 0s ago (extensions/goal-loop-display.ts:611,632) — dead parameter beside a real use: fmtElapsed clamps negative to 0, so clock skew prints "last approved 0s ago" instead of suppressing the age like auditorLastActivity does — fixed in 71b68b86
- [x] FIX: LOW: chatSafeDetailValue leaves nested machine-path husks (extensions/completion-summary.ts:161) — single-pass innermost-only paren strip turns "(log (/tmp/x.log))" into "(log )" which the empty-paren cleanup does not match — fixed in 558ebd71
- [x] FIX: LOW: clipSummaryValue slices UTF-16 code units (extensions/completion-summary.ts:90) — length/slice can split surrogate pairs, unlike the code-point-safe approval-render-store truncation fixed in v0.38.30 — fixed in 71b68b86
- [x] FIX: LOW: compactCompletionSummary mis-segments on inner label matches (extensions/completion-summary.ts:111) — first-occurrence indexOf per label, so a value containing e.g. "Tests:" steals the segmentation of every later label — fixed in 71b68b86
- [x] FIX: LOW: recoveryResume disjunct has no production writer (extensions/loops/goal-activation.ts:1554) — repo-wide grep finds no writer for recovery-resume.json (only tests plant it), so the explicitRecovery term is always false live and misleads recovery reasoning — fixed in 2ce99ba0
- [x] FIX: LOW: compareVersions equates prerelease with release (extensions/glla-version.ts:15) — parseInt("3-beta")===3, so 1.2.3-beta==1.2.3 and non-numeric parts force silent equality instead of unknown — fixed in 558ebd71
- [x] FIX: LOW: version-tail pins cover one branch plus hand-built literal (tests/display.test.ts:88) — active-goal branch only, while the live goal-ui wiring (runningGllaVersion + sidecar read) has zero coverage — fixed in 2016c54c
- [x] DECIDED 2026-09-10 (owner): keep the cleared-segment contract — idle sessions stay versionless. Rationale: on-demand `/glla version` already answers the version question; a permanent idle segment is noise without a supervision to supervise. Original DECIDE: idle sessions show no running version (extensions/goal-loop-display.ts:1152) — base undefined clears the segment before versionTail attaches, so empty sessions (the stale-session case v0.38.44 was built to expose) stay versionless; showing `glla: idle · vX` keeps the segment permanently visible for sessions with no supervision, keeping the clear preserves the cleared-segment contract but leaves idle stale sessions invisible

---
# Full-project audit pass — 2026-09-11 (v0.38.48, branch main)

Three parallel scout surveys (runtime/session/tasks, prompts/display/docs,
tests/packaging/release), all collected; every finding below was re-verified
against the tree by the orchestrator before recording. Disposed scout claims
(unproven, stale, or by-design) are recorded with rationale in the pass note,
not as findings. Append-only; boxes checked only after the fix commit lands.

- [x] FIX: MEDIUM: continuation prompt omits the v0.38.48 task-batch tool and pending-task completion gate (prompts/goal-loop-continuation.md:58,60,177,206) — the Available-tools list names only propose_task_list/complete_task/update_task_status/record_goal_judgment/pause_goal/complete_goal while extensions/loops/goal-tools.ts:2281 registers update_task_batch; the task-workflow line says track with complete_task/update_task_status and the complete_goal path never warns that open committed tasks refuse the claim before the auditor (extensions/loops/goal-tools.ts:569-578; extensions/task-batch.ts:150-152); the completion-communication voice still prescribes ~90-char one-bullet-per-label values while the v0.38.46 renderer budgets 200-char rich sections (extensions/completion-summary.ts:279-281,340-410)
- [x] FIX: MEDIUM: README verify steps omit the pending-task completion gate (README.md:247) — the numbered complete_goal path reads claim straight to detached auditor with no mention that open committed tasks (minus recorded deferrals) refuse before audit, so the first refusal surprises agents following the docs
- [x] FIX: LOW: user docs lag the v0.38.46-48 terminal voice and versions (INSTALL.md:34-38; README.md:27; docs/DESIGN-long-running-supervision.md:91-121) — INSTALL updating examples still show v0.38.45/v0.38.46 while package.json is 0.38.48; README and the supervision doc still describe the terminal surface as a flat six-label notification while chat/transcript/archive render the rich Done/Key Findings/Verification table/Next voice over the verbatim six-label machine record (extensions/completion-summary.ts:391-410,500-580)
- [x] FIX: LOW: completion-summary usefulness gate still uses first-occurrence segmentation while every projector uses last-occurrence (extensions/completion-summary.ts:49) — missingCompletionSummaryLabels matches via labelIndex/indexOf but compactCompletionSummary/completionSummaryLines/labelPositions segment on lastIndexOf, so a stolen label inside an earlier value passes the gate yet renders shifted; the v0.38.45 fix covered only the projectors (see findings line 446)
- [x] FIX: LOW: completing an already-complete task reports not found (extensions/loops/goal-tools.ts:2218,2257) — complete_task falls through to `Task <id> not found` when the id exists with status complete, masking typos vs state; update_task_status has the same blind spot for no-op same-status moves
- [x] FIX: LOW: sidecar writers bypass the pending sessionDir write boundary (extensions/approval-render-store.ts:69; extensions/glla-update-check.ts:58,88) — neither references stateRootPending while every core writer defers pending sessionDir resolution instead of creating a cwd fallback tree (extensions/glla-state-root.ts:84-94; tests/state-root-consumers.test.ts:77), so a pending contact can create the ambiguous cwd tree the boundary exists to prevent
